# Webiston — Active Roadmap

> **Read at the start of every session. Update at the start and the end.**
> Thin lines only. `[ ]` not started · `[~]` in progress · `[x]` done ·
> `[!]` blocked · `[>]` deferred.
>
> **This file is a tracker, not a plan.** Multi-phase work lives in
> `initiatives/`; completed initiatives move to `../archive/`. If an entry here
> needs more than 3 lines, it belongs in an initiative file.

_Last updated: 2026-07-31 — **the tools pass has started, with
`/tools/latin-cyrillic`** — the site's most-visited page. This was not a design
refresh: the engine was producing wrong Uzbek on ordinary words. Nine defect
classes fixed and covered by tests, the direction policy moved into
`@webiston/transliteration` so the web tool and all three extension surfaces
share it, the Chrome extension's context menu (dead since it shipped) made to
work, and chunking + the six info cards removed with the owner's approval.
Branch `refactor/infrastructure`.
**Self-review afterwards found four more, two of them mine** — see the rows
below._

---

## Snapshot

| Area | State | Evidence |
| ---- | :---: | -------- |
| Dependencies | ✅ | all latest; `pnpm outdated` → 1 (`hast`, deprecated, 0 imports) |
| Build & gates | ✅ | **all 9 gates green**; hooks 0.16s / 2.39s |
| Dev server memory | ✅ | Reported as "M4 Pro fans on, `next-server` 4.67 → 5.51 GB and climbing". Measured on a fixed 24-route sweep, same script each time: **startup 3,115 → 1,538 MB, peak 4,340 → 2,911 MB**. Three causes, one of which was NOT the memory: **(1)** a stray empty `package-lock.json` in `$HOME` made `findRootDirAndLockFiles` infer the workspace root as the whole home directory — pinned with `turbopack.root`; it fixed the build output but moved memory **0 MB**, so it is filed as correctness, not perf. **(2)** `next` 16.2.12 → **16.3.0** (memory eviction + dev FS cache, both default): startup −32%, peak −17%. **(3)** the real hog — `reactCompiler: true` runs **Babel** in a pool of Turbopack Node children sized to the core count: **11 processes / 1,827 MB peak**. `experimental.turbopackRustReactCompiler` → **1 process / 117 MB** |
| Standalone output | ✅ | Two independent bugs, both invisible until looked at. The wrong root made `next build` emit `.next/standalone/**Desktop/Next_projects/webiston**/server.js` — a deploy artefact nested under the author's home path. And `src/lib/mdx.ts:202` read a `let` reassigned in a loop, so Turbopack traced **the whole project** into the server bundle ("Dynamic filesystem access" warning); candidates are now tracked relative to `content/` and joined at the read. Warning **1 → 0**, `.next/standalone` 301 → **272 MB**, 272/272 pages still generated with 0 MDX read failures |
| Tool 14 — screen resolution | ✅ | Rebuilt on the suite standard. **1,098 → 1,675 lines** across 20 files (304 of the increase is tests, which the tool had none of), but the shape changed completely: a "control panel / output panel" two-column split (a layout for a tool that takes input — this one takes none) became viewport → breakpoint → measurements → devices → CSS, ordered by how often each is asked for. **7 files deleted.** Removed: a **demo-data button that loaded a fabricated 1920×1080 snapshot** into a page whose whole job is your real screen; six fake macOS window buttons; `!bg-white`/`dark:!bg-zinc-800` overrides on every button; framer-motion entrance animations; scroll-position tracking on an unthrottled listener; `getStats(translations?: any)`; two `as any` casts |
| Screen tool — correctness | ✅ | Five real bugs, each now covered by a test. **Aspect ratio** reduced by gcd, so 1366×768 printed **"683:384"** and a MacBook's 1512×982 printed "756:491" — now matched against 16 standards within 1.5% with a decimal fallback. **"Viewport ratio"** was `innerWidth / screen.width` — CSS pixels over device pixels, wrong by exactly `devicePixelRatio` on every Retina display; deleted. **Orientation** was `width > height`, now `screen.orientation.type` where it exists. **Resize** re-rendered per event; now coalesced into one `requestAnimationFrame`. **The hook spoke Uzbek** — `"Ekran ma'lumotlari yangilandi"` hardcoded, so `/en` showed it too |
| Screen tool — what it now answers | ✅ | The distinct job, versus DeviceInfo's display group: **the number your CSS sees**. Live viewport that updates as you drag the window edge; the active Tailwind breakpoint with *every passed* breakpoint marked (min-width queries stack — the part people get wrong); CSS px and device px in separate columns; 14 device presets listing **viewport** sizes, not marketing resolutions (an iPhone 15 Pro Max is sold as 1290×2796 and measures 430×932); and a ready-to-paste **range** media query. Suite 872 → **907 tests** (+25 unit for the maths, +10 integration driven through the rendered UI) |
| Screen tool — self-review | ✅ | Four gaps in my own code, found reading it back. **(1) The page shipped an empty body.** Every card was gated on `metrics`, which is `null` on the server, so `next build` emitted **0 card regions** and ~700px appeared at hydration, shoving the reference table and FAQ down. Components now take `metrics: ScreenMetrics \| null` and render labels with an em dash where the value goes — **5 regions and 15 pending dashes in the prerendered HTML**, both locales. Not a skeleton: the labels are real, only the numbers are pending. **(2) Megapixels disagreed with itself** — the panel counted device pixels, the JSON export counted CSS pixels, so a Retina Mac read 8.3 MP on screen and 2.1 MP in the file. **(3)** A redundant ternary branch in `BreakpointBar`. **(4)** `PresetList` was `tone="muted"`, which the ToolCard contract reserves for panels the visitor authors |
| Screen tool — what competitors charge for | ✅ | Two additions, both aimed at what the paid tools own. **Breakpoint scale switching** — Tailwind / Bootstrap 5 / MUI. Every free tool answers for exactly one scale, usually a generic 320/768/1024 list belonging to no framework; Bootstrap's `lg` starts at 992 and Tailwind's at 1024, MUI's `md` is 900 where Tailwind's is 768, and those gaps are precisely where a layout breaks. The generated CSS follows the choice. **A width probe** — type a width, or click a device row, and the breakpoint, device match and media query all answer for it while the readout keeps reporting the real window. Rotate swaps the axes, because half of responsive bugs live in landscape and you cannot rotate a laptop. [Viewport Resizer](https://tryhoverify.com/blog/5-chrome-extensions-for-responsive-testing/) and [Polypane](https://www.browserstack.com/guide/responsive-design-testing-tools) ($12/mo) charge for exactly this; [screenres.app](https://screenres.app/viewport-size-checker), the closest free competitor, does neither. Suite 907 → **921** |
| ⚠️ Site-wide: Uzbek chrome on `/en` | `[ ]` | Found while verifying: the header and footer render Uzbek `aria-label`s on every English page — `aria-label="Mavzuni o'zgartirish"`, `"Loyihani qo'llab-quvvatlash"`. Confirmed identical on the already-shipped `device-info`, so it is **not** a regression from this tool; the `Common` namespace resolves to `uz` in the root layout regardless of route. Affects all 34 tool pages and every book page. Not folded into a tool rebuild — it needs its own change |
| Same file read 3× per chapter | ✅ | `generateMetadata`, `layout.tsx` and `TutorialContent` each load a chapter independently and cannot see each other. Next dedupes `fetch()`; it does not dedupe `fs`. Counted on one `/books/fluent-react/2-jsx/what-is-jsx` request: `getMDXContent` **3**, `getTutorialInfo` **6**, `getTutorialNavigation` **8** — **17 disk round-trips**. Wrapped the three loaders in React `cache()` → **1 / 3 / 3 = 7**, a **59%** cut. Not 1 each because Next runs `generateMetadata` in its own cache scope — that is the API working as designed, not a leak. `getAllTutorialPaths` deliberately left uncached: it runs in `generateStaticParams`, outside any request scope |
| `@next/mdx` removal — honest scorecard | ⚠️ | Measured A/B, two builds each way: client JS **6,884 KB → 6,884 KB**, chapter JS **933,725 B → 933,725 B**, `node_modules` **1,067,072 KB → 1,067,072 KB**, build wall **~16.4–17.4s** both ways. **Identical.** `@next/mdx` ships nothing to the client and `@mdx-js/*` stays installed via `next-mdx-remote` anyway. The removal bought clarity and two fewer declared deps — **not** speed or size. Recorded here so nobody re-derives the wrong conclusion from the diff |
| 3 chapters "failing" to compile | ✅ | **Not what the log made it look like.** The build printed `Error parsing MDX, falling back to markdown parser` three times, all in `ai-engineering`. The pages were never broken: `TutorialContent` → `MDXContent` compiles with `remarkMath` + `rehypeKatex` and all three render (**103 / 51 / 22** KaTeX spans in the prerendered HTML). The failing compile was a **second** one, in `generateMetadata`, which called `serializeContent()` — remark, rehype, acorn, JSX codegen over the whole chapter — and then read exactly one property off it: `.frontmatter`. That path has no `remark-math`, so `^{-\frac{1}{n}}` hit the MDX parser as a JSX expression. Now `matter(contentText).data`, the same call `TutorialContent` always used. **226 discarded MDX compiles removed from the build**, errors 3 → **0**, and 226 lines of `Using MDX parser` log noise → 0 |
| `@next/mdx` was dead config | ✅ | Wrapped around `next.config.ts` to compile `.mdx` at build time in an app with **zero** `.mdx` under `src/` — all 226 chapters are read from `content/` at request time by `next-mdx-remote/rsc`. It cost a Turbopack webpack-loader rule, `pageExtensions: ['md','mdx']` widening Next's route scan, `@mdx-js/loader`, and a `providerImportSource: '@mdx-js/react'` pointing at a package that was never a declared dependency. All removed; build **272/272 pages, 0 warnings** |
| Supply-chain exemptions | ✅ | `minimumReleaseAgeExclude` held 10 packages from the 2026-07-29 upgrade. All had aged past the 24h window and `pnpm install` passes without the block — the condition its own comment set. Deleted, so `minimumReleaseAge: 1440` applies to everything again |
| Two copies of framer-motion | ✅ | `packages/ui` asked for `^12.43.0`, the root for `^13.0.0` — **both shipped**. framer-motion carries React context (`PresenceContext`, `LayoutGroupContext`), so an `AnimatePresence` from `@webiston/ui` could not see a `motion.div` from `src/`, and the bundle paid twice. Aligned to `^13.0.0`; `packages/ui` now resolves the same copy as the app |
| One name for one library | ✅ | `framer-motion` (22 imports) **and** `motion` (1 import) were both dependencies. `motion@13` is a re-export whose only dependency **is** `framer-motion@13`. The one `motion/react` import moved over; `motion` removed |
| Unused dependencies | ✅ | **9 removed, 77 → 68**: `motion`, `file-saver`, `fs-extra`, `@types/fs-extra`, `hast`, `@types/hast`, `rehype-pretty-code`, `unist-util-visit`, `autoprefixer` (Tailwind v4 prefixes itself and `postcss.config.mjs` never listed it). Each verified at 0 references across `src/ packages/ apps/ scripts/` and every config file. `katex` and `@testing-library/jest-dom` looked unused to the same scan and were **kept** — one is imported by `globals.css`, the other by `vitest.setup.ts` |
| `@types/react` resolved twice | ✅ | Surfaced by the reinstall, latent before it. Root `^19.2.18` vs extension `^19.2.17` → two copies, Radix peers resolved against both, and `packages/ui` (which declares neither) linked the wrong one: **every `asChild` component failed typecheck** with *"Two different types with this name exist, but they are unrelated"*. Fixed with `overrides` in `pnpm-workspace.yaml` — the duplication is peer resolution, not a direct dependency anyone could bump |
| `middleware` → `proxy` | ✅ | Next 16.3 deprecated the file convention. `git mv src/middleware.ts src/proxy.ts`, default export unchanged, `next-intl/middleware` import path unchanged (that is the library's entrypoint, not Next's file name). Deprecation warning **1 → 0** |
| Hydration mismatch | ✅ | `<body>` diffed on `cz-shortcut-listen` / `bis_register` / `__processed_<uuid>__` — browser-extension attributes, not our markup. `suppressHydrationWarning` applies to **one element**, so the one on `<html>` never covered `<body>`; added there |
| Repo hygiene | ✅ | `git ls-files -i -c` → 0 |
| Package boundaries | ✅ | `packages/ui` `@/` imports 3 → **0**; one definition, re-exported |
| Duplicated types | ✅ | `PasswordSettings` declared twice in one file · `MetaData` ×3 with one already **drifted** · `CapturedMedia` ×4 → all consolidated. TS merges identical interfaces, so no gate saw any of it |
| Duplicated formatters | ✅ | 5 copies → `src/lib/utils/format.ts`. Had drifted into **visible** inconsistency: `formatDuration(65)` was `01:05` in one place and `1:05` in another **within the same tool**; `formatFileSize(0)` printed `NaN undefined` |
| `pnpm check` | ✅ | **81 → 0 errors.** First time this gate has ever passed |
| i18n parity | ⚠️ | `pnpm i18n` gate added; red on 8 dead `en`-only keys pending approval |
| SEO integrity | ✅ | fabricated ratings + invented reviews **deleted** (18 files, 416 lines); repo-wide grep → 0 |
| Canonical / hreflang | ✅ | every page self-canonicals; verified in served HTML for `/`, `/en`, tools, books |
| OG share cards | ✅ | `/api/og` implemented (`next/og`) — was a 404 on all 229 book pages |
| Static rendering | ✅ | **0 → 266 routes prerendered.** 34 tool pages (17 × 2 locales) + 228 book chapters + home + index pages. Only `/api/og` stays `ƒ` — it reads `searchParams`. `cacheComponents`/PPR deliberately rejected, see initiative |
| Payload | ⚠️ | search index on dialog-open (**0** requests on page load) · logo **209 KB → 5.5 KB** · **CLS measured 0** · tool-page HTML **−43%** via scoped i18n. Left: `images.unoptimized` (**25 MB of raw book figures**) |
| Soft 404s | ✅ | `/books/**` returned 200 for non-existent chapters *and* any unknown book id; both now 404 |
| Boundary contrast | ✅ | **NEW `--border-strong`.** `--border` measures **1.32:1** light / **1.33:1** dark — WCAG 1.4.11 needs **3:1** for a boundary that identifies a component. Solved: 3.16/3.02. `--border` documented decorative-only |
| `pnpm contrast` (again) | ✅ | **Was vacuous on the one token that mattered.** `--border` absent from `PAIRS` *and* unparseable (alpha) → would have `SKIP`ped. Gate now composites alpha in gamma-encoded sRGB. **32 → 36 pairs** |
| Homepage | ✅ | Search overlap **−25px → +8px gap**. nextjs.org motif (180 lines, ~4s unguarded motion) → drifting grid + CSS `view()` reveal, **zero JS**. 8 junk classes were shipping in the `<h1>`. Cards had **no background** (alpha 0). Header seam fixed via a frosted bar after a scroll-driven fade measured **inert** (`timelineProgress: 0%` at every scrollY) |
| Light-mode CTA | ✅ | The hero's primary button was **a white pill on a white page** — `ButtonLink` pinned `bg-white text-black` in both schemes. `bg-foreground text-background` flips together: **17.4:1 / 18.7:1** |
| Hero copy | ✅ | **Reverted to the owner's original** — the palette visual now answers "what is this site", which was the whole case for the rewrite. Split into `titleLead`/`titleAccent` |
| Logo | ✅ | "Terminal" mark from the brand exploration, pure markup — PNG request gone from every page. **Favicon still old — needs image generation** |
| Header seam | ✅ | Phase 7's frosted bar treated the symptom. Root cause: the glow was anchored at `top: 0` — the exact strip the header covers — so the header was the one band WITHOUT the tint. Light moved down; hairline scoped off hero pages with `:has()` |
| Hero grid | ✅ | `4rem` (64px, graph paper) → `clamp(7rem, 10vw, 14rem)`. nextjs.org spaces its rules ~500px |
| Hero legibility | ✅ | "Beam looks like it's over the text" — z-order was already correct (`elementsFromPoint` → `H1` first). Perception, not a bug. Fixed with a `--background`-derived scrim; paint order `grid → scrim → aurora` keeps the glow |
| Refresh animation | ✅ | Lines **draw** in on load — `scaleY`/`scaleX` from top/left, 180ms stagger, 2.1s. The first attempt only *faded* (invisible on a 1px line) and the scrim shipped in the same change was dimming it. Verified on a real load: caught at `scaleY(0)`, `state: running` |
| Hero motion | ✅ | Drift was running all along but **invisible**: `--border` is 1.32:1, so the lines were. New `--hero-line` token + a beam masked TO the grid (`mask-composite: add`) that lights the lines as it passes. **Zero JS** — 0 framer-motion refs in the homepage HTML |
| Missing i18n key | ✅ | `JwtDecoderPage.InputPanel.clear` added to both locales — the dev overlay's "1 Issue". Build `MISSING_MESSAGE` **2 → 0** |
| Reduced motion | ✅ | The only `prefers-reduced-motion` block on the site belonged to **Sonner**; zero of our own animations were guarded. Now gated, verified in compiled CSS |
| Design tokens | ✅ | **All 5 phases shipped.** 3-layer, hue 217°, 32/32 contrast PASS, ratchet live. **5,401 → 2,600** hits · `dark:` 1,967 → **570** · tokens 170 → **1,658**. Of what's left, 629 = parked tools, 195 = colour *data* |
| Tests in `src/` | ⚠️ | **79** across 5 files — suite 207 → **286**. `src/` had zero. Found **3 real bugs**: `rgbToHex` emitting invalid CSS, `truncateText` exceeding its own limit, and the password generator on `Math.random()` |
| Book landing pages | ✅ | `/books/<id>` ×3 rewritten. Was a `list-disc` bullet list of **the same links the sidebar already showed**, plus "pick a topic from the sidebar" — zero information of its own. The right rail was **two dead `href="#"` links** (headings had no `id`). Card grid uses **container queries**: at a fixed 1024px viewport, collapsing the sidebar takes the column 344px → **632px** and the layout follows (1 col → 2 × 308px) — a viewport breakpoint cannot see that. **17b:** owner rejected the first cards — the bottom `border-t` drew a rule under one-word titles; cards now match the homepage order exactly and their third line previews the chapter's own first 3 topics |
| Reader shell alignment | ✅ | Owner: *"chapdagi sidebar header bilan teng emas"* — correct, and measured. Header logo at **x=64**, sidebar rows were at **80** and their text at **92** (a layout `pl-4` stacked on each row's `pl-3`). Now rows at **64**, flush with the logo AND the footer's first link. Both rails inset the same 12px; right rail's right edge = **1536** = the header's |
| Sticky rails | ✅ | `TutorialLayout` hardcoded `top-[3.5rem]` (56px) while the header measures **65px** — both rails sat **9px underneath the bar**. Now `top-(--header-height)`; verified on a 5,664px chapter at scrollY 1500: `asideTop` = `railTop` = **64**, flush against the border |
| Reading page payload | ✅ | **Barrel imports were the whole story.** Chapter JS **383 → 340 KB gz**, client modules in the route manifest **30 → 16**. `CodeBlock` imported `CopyButton` from `@/components/shared`; all 16 `components/ui/*` shims re-exported the `@webiston/ui` ROOT barrel; and the last leak was a **constants** file (`ui-constants.ts` → `@webiston/ui`) reached via `Footer` on every page. lucide's chunk **111 → 15 KB** once tree-shaking could see through it. Also: `packages/ui`'s `"./composites/*"` export mapped to files that never existed |
| FlexSearch eager load | ✅ | **74 KB gz on all 269 routes.** `Search.tsx` statically imported the engine module, which imports the library AND runs `new SearchEngine()` at module scope. New `lib/search/load.ts` (memoised `import()`, no static import). Verified: `flexsearch` in **0** eager chunks, search still returns 15 hits |
| ToC anchor scrolling | ✅ | Clicks landed **165px off**. The handler used `element.offsetTop`, which is relative to the nearest POSITIONED ancestor — `TutorialLayout`'s root is `relative`, so measured `offsetTop` 522 vs true 587, then `-100` on top. Headings already had `scroll-margin-top: 80px`; deleting the handler lets native anchors do it right. Verified `scrollY` 1685 = expected, clears the header by 15px |
| Book figures | ✅ | **Not in the bundle** — 0 base64 rasters in any chunk; `.next/static/media` is fonts only. `public/` = **26 MB / 106 files**, served per-request, only on the page that uses them. Figures now show a `bg-muted` + shimmer skeleton in the already-reserved box instead of an empty hole (one chapter pulls **1,549 KB** of PNG). Real blur LQIP needs `sharp` + a build step — `placeholder="blur"` **throws** without `blurDataURL` for a string src (verified in Next's source) |
| ToC rail indicator | ✅ | Three attempts. A 2px marker **cannot** be centred on a 1px `border-l` track — that is why it looked like it hung off the line. Track and marker are now two bars with identical geometry (`left-0 w-0.5 rounded-full`), concentric by construction (both `left: 1292`, `width: 2`). Hovering the active row used to **grey out its own marker** (`hover:border-*` beats `border-primary` — different variants, `tailwind-merge` can't dedupe). One bar now animates `top`/`height`: verified on a 2-line heading, `top: 276 / height: 52` against a 52px row, 0px off top and bottom |
| ToC follows the reader | ✅ | 27 headings vs a `max-h-[calc(100vh-8rem)]` box meant the highlight sat below the panel's fold. `keepRowVisible()` scrolls **only the rail's own box**, only when the row has left it — not `scrollIntoView()`, which walks every ancestor and would drag the page. Verified: computed target **317** = the rail's exact max scroll |
| 404 / error pages | ✅ | **The site had no 404 at all.** Every unmatched URL — `/x`, `/books/unknown`, `/books/<b>/unknown`, `/en/x`, `/tools/x` — served Next's built-in "This page could not be found.", no chrome, no way back (verified on the production build). The three hand-written files under `books/` **never ran**: `dynamicParams = false` rejects unknown params at the ROUTING layer, so a segment `not-found.tsx` only fires when `notFound()` is called during a render that happens — which for a prerendered corpus it never is. New branded `app/not-found.tsx` (+ `noindex`, verified in served HTML) and `app/global-error.tsx` (inline-styled — the stylesheet lives in the layout that failed). Prerendered **269 → 270** |
| Contextual 404s | ✅ | Owner: a mistyped chapter should not eject you from the book. Correct, and Next supports it — `not-found.js` renders INSIDE its layouts. `dynamicParams` `false` → **`true`** unblocks it (costs no prerendering: **228** book HTML unchanged, route still `●`). Now a ladder: bad chapter → book shell kept (sidebar 7 items, breadcrumb, header, footer — verified in the DOM) · bad book → header+footer + the three real books · anything else → global. Split decided in the layout, whose `getTutorialInfo` guard also bounds the render surface |
| 404 metadata | ✅ | `generateMetadata` title-cased the URL, shipping `<title>ModelingXXX | AI Engineering…</title>` + canonical + OG image for a 404. Gone. Honest scope: Next discards page metadata once it 404s, so the served title is the neutral site default, not a custom one; `noindex` is auto-injected and verified present |
| 404 centring | ✅ | Owner reported it twice and was right — the description sat **64px** left, exactly `(576 − 448) / 2`, an auto margin not resolving. Fixed structurally with `flex flex-col items-center` so the parent owns alignment. Measured: six children, all text centres at **800** on a 1600px viewport, **spread 0px** |
| `dangerouslySetInnerHTML` | ✅ | Audited all ~70. **~65 are JSON-LD from hardcoded consts** — no injection path; the `<` → `\u003c` hardening belongs in the tools pass (60 of them are tool pages). **1 is the search snippet** — the only place a `content/**` string becomes markup — now escapes text BEFORE inserting `<mark>`. Measured: 1,078 docs, **0** with script/img/iframe/handler; `<script>` in `content/` is all inside code fences the indexer strips (3 → 0). **No DOMPurify needed** — nothing renders third-party or user-submitted HTML |
| Token baseline | ✅ | Re-frozen **2,605 → 2,486**: this session's −123 locked in, +4 for `global-error.tsx`, which cannot use tokens by definition (`tokens.css` is imported by the layout whose failure renders it). Values named in `FALLBACK_PALETTE` per the gate's own instruction |
| Loading states | ✅ | No `loading.tsx` anywhere, and none needed — every route is prerendered so there is no server wait, and client navigation is covered by `NextTopLoader`. `forbidden`/`unauthorized` correctly absent (no auth) |
| Sidebar collapse | ✅ | Button aligned to nothing when collapsed (`left-[calc(100%-0.5rem)] translate-x-4` = 8px into the gutter, then 16px back). Now `-left-2.5` — half the 40px control minus its 20px icon — so the **icon** lands on the content edge: measured **64 = 64** vs the logo. Animation `transition-all 500ms` → `transition-[width,border-color] 300ms` |
| Prose reflow on collapse | ✅ | Root cause was not duration: the column genuinely grew **864 → 1,152px**, so every paragraph re-wrapped on every frame, plus a `translate-x-4` + `pl-12→pl-8` + `max-w` swap on a separate 500ms transform. New `--reading-measure: 54rem` = exactly the sidebar-open width, so the extra space becomes margin. Measured across a collapse: paragraph **816×168 → 816×168**, identical, aside 288 → 0. Better typography too — 1,152px is ~150 chars/line |
| Header chrome | ✅ | Top loader was `#3b82f6` (Tailwind `blue-500`), the one thing shown on **every navigation** painted in someone else's blue → `var(--primary)`. Theme icon was wrong pre-mount (`useTheme()` is `undefined` on the server → Moon always rendered → visible flip on hydration in light mode) → both icons in the DOM, CSS picks off `.dark`. **framer-motion gone from the header**: chapter JS **340 → 299 KB gz** |
| Sidebar collapse | ✅ | **Removed, owner approved.** Once `--reading-measure` capped the column the toggle bought nothing — the prose could not widen, so it only hid navigation. No major docs site ships one on desktop |
| Reader shell → server | ✅ | `TutorialLayout` is now a **Server Component**. Two things held it client: the collapse state, and a `useEffect` hydrating the nav store that was **duplicate** — `NavigationStoreInitializer` already does it as an immediate sibling with the same arguments. Client modules on the chapter route **16 → 15** |
| Theme switch animation | ✅ | Instant was **unfixable in CSS**: `disableTransitionOnChange` injects `* { transition: none !important }` during the swap — it killed 18c's rotate/scale AND the framer-motion version before it, so that dependency animated nothing. The flag is right (1,600 token-driven elements would smear at once). Answer: animate **one** thing — `document.startViewTransition()`. First shipped as a `clip-path` circle from the button; owner called it gaudy and was right, so it is now the browser's plain cross-fade at **180ms**. Verified: `circularWipeStillInCSS: false`, `crossFadeDuration: 0.18s`. Falls back to instant on Firefox / reduced-motion |
| ⚠️ Second retraction | ✅ | I recorded that the toggle's first click "did nothing visible" under `enableSystem`. Checked: with `defaultTheme="dark"` and empty storage next-themes resolves `theme` to **"dark"**, not `"system"`, and our UI has no System option — the branch is unreachable. `resolvedTheme` is still correct for a binary toggle but fixed no observable bug. Recorded in the component |
| ⚠️ A finding I retracted | ✅ | I reported the ToC coming up **empty after client-side navigation**. It does not. Cause: the `output: standalone` server **404s every client chunk** (`.next/static` is not copied in — documented, on us), plus a probe matching `"Ushbu sahifada"` against an `uppercase` element. Disproved by rebuilding without the `key` and re-testing on `next start`. The `key` is kept on honest grounds (`cacheComponents` + `<Activity>` would break the mount-only assumption), not as a bug fix |
| Rendering model answered | ✅ | **0 JS is impossible in App Router** (React runtime + RSC payload + router always ship) — Astro is the only 0-JS answer and that's a rewrite. These pages are **SSG, not SSR**, which is correct for a fixed corpus — do not "add SSR". RSC shape was already right; the defect was leakage across the boundary. See `reference/architecture.md § 5b` |
| `pnpm tokens` blind spot | ⚠️ | `PALETTE_RE` has no `black`/`white`, so `hover:text-black dark:hover:text-white` is **invisible to the ratchet**. **124** such hits repo-wide. Widening the regex moves the ratchet for everything — needs a decision |
| CI | ✅ | `.github/workflows/ci.yml` — all **10** gates, one job, corepack-pinned pnpm. First run will be red on `i18n` (the 8 dead keys) |
| Engine: Uzbek orthography | ✅ | **307 tests passed while `ishchi` came out as `ищи`.** Every case in the old suite was a synthetic fragment ("shch", "ShH"), never a word. Fixed and covered by a real corpus: `sh`+`ch` boundary (ishchi → **ишчи**, 4/4 occurrences in the repo's own Uzbek prose were this, 0 were Russian щ) · tutuq belgisi after a consonant (san'at → **санъат**, was саньат) · the `-tsiya` family (informatsiya → **информация**, was информатсия) · `ц` positional (функция → **funksiya**, was funktsiya) · soft sign in loanwords (фильм → **film**, was fil'm, and mid-word it round-tripped to филъм) |
| Engine: protection layer | ✅ | Measured **10 of 244** everyday Uzbek words and **12 of 66** common loanwords came back still in Latin — `buni`, `bunga`, `bundan`, `o'sha`, `tan`, `tanga`, `sin`, `test`, `format`, `virus`. Three causes: `\b` splits `o'sha` because JS treats the apostrophe as a separator, suffix expansion turns 3-letter entries into common words, and absorbed loanwords were on the list. Now **0 of 244** and **1 of 66** |
| Engine: robustness | ✅ | ReDoS: 273 KB of prose with unclosed `<div` took **6,605 ms → 34 ms** (`[^>]*` scanned to end-of-input per opener). Placeholder forgery: `React \0 0 \0 salom` returned **"React React salom"** — text could impersonate a placeholder and get another span substituted into it |
| Engine: direction detection | ✅ | One link made a Cyrillic article count as Latin (`Батафсил: https://gazeta.uz/…` → 8 Cyrillic vs 34 Latin). Detection now votes on MASKED text, so URLs, emails and code — which are never converted — do not choose the direction. Also collapsed two disagreeing detectors into one |
| Shared conversion policy | ✅ | `resolveDirection` / `convert` / `convertWithPreference` / `oppositeDirection` in the package. Four surfaces had four answers: the web tool only re-detected when the length changed by **>5 characters** (typing Cyrillic never switched; deleting a paragraph silently overrode a manual choice), the popup used a 3-state preference, the popover discarded the user's choice on every keystroke, and swap meant three different things |
| Extension: dead context menu | ✅ | `background.ts` sent `REPLACE_SELECTION`; `content.ts` listened only for `CONVERT_SELECTION`. **All three right-click entries computed a conversion and threw it away** — since the day they shipped. Now handled: writes into the field or contenteditable, falls back to the clipboard |
| Extension: "Almashtirish" | ✅ | The Replace button, pencil icon and all, called `clipboard.writeText` and closed — identical to Copy. It now replaces via `setRangeText` / Range surgery, and the theme toggle no longer needs two clicks on a light-mode machine |
| Tool: data loss | ✅ | "Hammasini yuklash" wrote **only the selected chunk** and named the file `_full`; every chunk downloaded under the SAME name because `_part1` was eaten by the extension-stripping regex. Chunking removed entirely — the engine does 50,000 chars in **9.6 ms** and 1 MB in 148 ms, so it was machinery for a problem that does not exist. The 200 MB limit (also published to Google) is now a tested 10 MB |
| Tool: client boundary | ✅ | `page.tsx` imported `@/modules/tools`, whose barrel re-exports all 21 tool modules, every one `'use client'`. Measured: **92 → 50** client modules, **22 → 1** tool modules, **664 → 358 KB gz** of JS. HTML grew 24.5 → 28.7 KB gz because the alphabet table and FAQ are now server-rendered. **The other 16 tool routes still import the barrel** |
| Tool: honesty | ✅ | JSON-LD advertised **99.9% accuracy** (while the engine failed on `ishchi`), a **200 MB** limit no browser can honour, and a **PDF export that does not exist**. The six FAQ answers were emitted as `FAQPage` markup and rendered nowhere — a guidelines violation on the top-traffic URL. FAQ is now visible and reads the same i18n keys the schema does. `<meta keywords>` 144 → 15 entries |
| Tool: UX | ✅ | Direction is **Avto / → Кирилл / → Lotin**, matching the extension. Drop a file anywhere on the page (the handlers existed, wired only to a 296-line modal that closed itself after 800 ms). ⌘/Ctrl+Enter copies, Esc clears — there were **zero** key handlers before. Empty panel offers paste + sample, measured above the fold at y=474 (they were at y=878, below it). Mobile: panel min-height 400 → 200 and the tool header 36px → 24px, so the input is on the first screen |
| Tool: dead code | ✅ | Owner approved. `utils/detect-script.ts` (115 lines, 0 importers, and it disagreed with the engine) · `FileUploadZone.tsx` (200 lines, never rendered) · `downloadAllChunks()` · the two Russian samples with no UI path · 16 dead i18n keys |
| ⚠️ Self-review: my own quadratic | ✅ | The `ts` seam check did `text.slice(i + 1).toLowerCase()` on EVERY "ts", copying the rest of the document each time. Measured: **21 ms at 30 KB, 3.2 s at 480 KB, 49 s at 1.9 MB**, against a flat 0.1 ms/KB for text without "ts". Bounded to a 7-character window — the longest suffix plus one: **48,972 → 252 ms**, and the ms/KB curve is now flat. Regression test asserts 8× input costs under 20× time |
| ⚠️ Self-review: my own lookbehind | ✅ | I replaced `\b` with `(?<!…)` and shipped a **module-scope** `new RegExp` — on Safari before 16.4 that throws while the module evaluates and the whole page dies. Tried consuming the character instead; that made the vocabulary branch start one position earlier than the URL/email branches and it won the leftmost race, splitting `info@webiston.uz` into `info@webiston.уз`. Settled on `\b` plus a two-character check in the replace callback |
| Apostrophe as a letter | ✅ | `o'` and `g'` are single LETTERS; `\b` splits them. Scanning **all 226 Uzbek chapters, 22,928 distinct words**, found three protected acronyms exposed this way, all inside common words: `o'sha`→`ўsha` (sha), `ko'rsa`→`кўrsa` (rsa), `o'ram`→`ўram` (ram). Also added the Uzbek convention for foreign words — `Google'da`, `TikTok'gacha`, `LLM'larni`, `rendering'ning` — which had never worked. Corpus partial leaks **0**; protected terms 440 → **740** |
| /en rendered Uzbek | ✅ | **Site-wide, pre-existing, found while reviewing.** `setRequestLocale` writes into a React.cache value `getRequestConfig` never reads back: on /en, `getLocale()` returns **"uz"** while `params.locale` is "en", so every `getTranslations("…")` served the Uzbek bundle. Confirmed on the prerendered HTML of `/en/tools/json-formatter` too. Fixed on this route by passing the locale explicitly (what `[locale]/layout.tsx` already does for metadata); `LocaleMessages` now takes an optional `locale`. **The other 16 tool pages are still affected** |
| Radio group that was not one | ✅ | `role="radiogroup"` + `role="radio"` on buttons, with a comment claiming arrow keys "come free". They do not — that pattern also owes a roving tabindex and key handling. Replaced with real `<input type="radio">` in a `<fieldset>`: correct grouping, correct announcement, arrow keys, and it works before hydration |
| Verified end to end | ✅ | PDF import through the **bundled** worker (`/_next/static/media/pdf.worker…`, **0 third-party requests**) · drop-a-file · swap (labels and the radio flip together) · Esc · ⌘Enter feedback · `/en` in the built HTML · the other five tool pages unaffected by the shared `ToolHeader` / `DualTextPanel` edits |
| Segmented control, shared | ✅ | The direction pills JUMPED because the highlight was a style ON the active option — there is nothing to animate between two elements appearing and disappearing. New `SegmentedControl` in **`@webiston/ui`**: ONE indicator that slides on `transform`+`width`, measured with a ResizeObserver, real `<input type=radio>` in a `<fieldset>` underneath. Verified by geometry (`translateX(158.227px)`, width 73.1 = the active label exactly) — the Browser pane does not tick CSS transitions, so a transition-off probe was used to prove the target applies |
| Accordion never animated — anywhere | ✅ | `packages/ui/.../accordion.tsx` has referenced `animate-accordion-down` / `-up` since it was added, and **the keyframes were never defined**. Every accordion on the site opened instantly. Defined in `tokens.css` off `--radix-accordion-content-height`, guarded for `prefers-reduced-motion` like every other animation here. The FAQ now uses that shared primitive (server reads the messages, a client leaf does the opening) instead of my hand-rolled `<details>` |
| Swap did nothing | ✅ | Reported as "umuman ishlamayapti". It works with text — but it was ENABLED on an empty panel and returned early, so the first thing a visitor clicked was a dead control. Now `disabled` until there is something to swap, in `DualTextPanel`, so every tool gets it |
| Dark-mode tables, site-wide | ✅ | `base.css` had `td { border: 1px solid #ddd; text-align: left !important }` as a bare ELEMENT rule — a light grey with no dark variant on every table on the site, which is the grid-of-junk look, plus an `!important` no component could beat. The only consumers are 23 book chapters with markdown tables, and those are already styled with `border-border` in `MDXContent.tsx`. Rule deleted; book tables re-checked and now draw on `--border` (measured `lab(100 0 0 / 0.12)` in dark). Token baseline **2,486 → 2,399** |
| Page width | ✅ | The tool was `max-w-7xl` (1280px) while the header and footer are `max-w-[1536px]` — the content edge missed the chrome by 128px a side. Now aligned: control bar, panels, table and footer link all measured at **x=32** |
| Indicator sat 5px off its option | ✅ | `getBoundingClientRect()` returns the BORDER box; an absolute child with `left:auto` starts at the CONTENT box. Measuring against the former shifted the highlight by border+padding — measured 5px, which pinned the last option's pill flush against the track's right edge. Now offset from the content-box origin: `deltaLeft 0 · deltaRight 0 · 4px` clear of the edge on every side |
| Indicator replayed from the first option | ✅ | The server has no layout, so the markup ships at `translateX(0)`; the browser paints that and the layout effect corrects it, which a CSS transition treats as a MOVE. Every fresh render of the tree — switching locale is one — slid the highlight in from "Auto". Transitions now enable one frame after the first measurement: arrival is instant, moves animate. framer-motion's answer to this is `initial={false}` — the same idea, at the cost of a bundle |
| Accordion had no pointer or hover feedback | ✅ | A Radix trigger is a `<button>`, whose UA cursor is `default` — every accordion on the site failed to say it was clickable. `cursor-pointer` added to the shared primitive. The FAQ card moved to a shared `FaqAccordion` composite in `@webiston/ui` with the padding on the TRIGGER, so its hover background spans the full card (measured 1214 of 1216px) instead of stopping short of the edges |
| File progress bar stuttered | ✅ | Real progress arrives in lumps (DOCX 0→50→100, PDF per page) and a CSS transition animates each lump then FREEZES. New shared `ProgressBar` in `@webiston/ui` eases toward each report on one time constant and creeps forward on a slower one so it never sits still, capped part-way to the next milestone so it cannot outrun the truth. Moves with `transform`, written straight to the node. The hook also holds the finished bar 500 ms — it used to unmount in the same commit that reported 100% |
| macOS traffic lights in the tool panels | ✅ | Three raw palette classes imitating window controls that do not exist, in the first place the eye lands, with a dead `hover` field nobody read. Replaced in `DualTextPanel` (**9 tools**) with the kicker mark the section headings use, carrying one real bit: the accent fills the panel holding the ANSWER. `MACOS_DOTS` stays for `HeroPalette` / `TerminalInput`, where a window chrome is the actual subject |
| Live counters ran through a spring | ✅ | `StatsDisplay` (14 consumers) drove its numbers with `NumberTicker`, a framer-motion spring — right for a hero stat that counts up once, wrong for a counter that changes per keystroke: it showed numbers that were never true and settled a beat late. Now plain `tabular-nums`. The ticker also wrote `text-black dark:text-white`, which outranked the muted colour of the row it sat in — now `text-foreground` |
| Preserved terms were invisible | ✅ | The engine deliberately keeps links, e-mail, code and ~740 technical terms, and from the output that is indistinguishable from a bug. New `findPreservedTerms` in the package (deduped, first-seen order, 4 tests) and a footer line in both locales: "O'zgarishsiz saqlandi: React, GitHub, webiston.uz +1" |
| Control bar was a card around a void | ✅ | A bordered card wrapped two groups that are not one thing (direction switch, file actions); the band between them measured **535px — 44% of the row**. An empty card reads as "something is missing"; the same gap on the page is just whitespace. Card chrome dropped: row **66 → 40px**, panels are the only cards, one nesting level instead of two |
| Tool `h1` at magazine scale | ✅ | 36px against 16px panel headings — a 2.25× jump on a utility page, a tenth of the first screen spent on a title nobody came to read. `sm:text-4xl` → `sm:text-3xl` in `ToolHeader` (**21 tools**). The h1 stays: it is the page's one indexable heading |
| Extension popup: same jumping tabs | ✅ | The popup had its OWN hand-rolled direction tabs with the highlight on the active option — the identical defect just fixed on the web, plus no radio semantics and no keyboard support. Now the shared `SegmentedControl`; `@webiston/ui` added as a workspace dep |
| Extension: Tailwind never scanned the shared package | ✅ | Found while wiring the above: the built CSS contained **no `bg-primary` and no `bg-muted` at all**, so any shared component would have shipped unstyled. Fixed with `@source`, scoped to the one composite — the whole `packages/ui/src` tree costs 67 KB of CSS here vs **18 KB** scoped |
| Extension colours were a different product | ✅ | `popup/style.css` hardcoded its own palette — `--primary: #0ea5e9`, a sky blue, against the site's teal `oklch(0.745 0.115 217)` — and had **no `@theme` block**, which is why every element carried an inline `style={{background: "var(--primary)"}}`. Values replaced with the site's real oklch tokens and bound to Tailwind's colour names. Still a COPY — see *Next up* |
| Extension used two browser APIs | ✅ | `background.ts` and `content.ts` use WXT's `browser`; the popup alone reached for the untyped `chrome` global. Those three lines were the extension's **only** type errors (4 → 0) and would have had to be rewritten for Firefox |
| User exception list | ✅ | The gap every competitor fills and we did not. `protectContent` now takes user terms (escaped — `.*` typed into the field protects those two characters, not the document), threaded through `toCyrillic`/`toLatin`/`convert`/`convertWithPreference`/`findPreservedTerms` as an optional `preserve`. Terms get the same Uzbek suffix treatment as the built-in vocabulary but with **no length floor** — a hand-typed entry is a deliberate statement. Bounded at 200×64 chars, one compiled regex per distinct list. Store at `version: 3` with a migration. 7 tests incl. a linearity assertion; suite **418 → 425** |
| Exceptions dialog was unfinished | ✅ | Icon-to-label spacing was doubled — `Button` already sets `gap-1.5`, and an `ml-2` on top of it read as a gap and a half (fixed on all five buttons I had touched). The empty line sat on the input; the list had no ceiling, so past ~40 entries the modal grew off-screen and took the close button with it. Now: reserved-height notice row, `max-h-[38vh]` scrollable list, `n / 200` counter, and every silent rejection surfaced — duplicate (case-insensitive) and list-full each say so instead of looking like a dead button. Verified at 200 entries: modal 576px in a 720px viewport, list 274/1785 scrollable, a 64-char chip does not overflow |
| Clear-all, without a modal on a modal | ✅ | Two-step in place: the button arms, then confirms, then **disarms itself after 4s**. A dialog stacked on a dialog is a stack Escape unwinds one layer of, and it covers the very list it is asking about. Reopening resets the draft, the notice and the armed state |
| Mobile toolbar cost a row | ✅ | Three labelled buttons wrapped onto a second row at 375px. Labels are `sr-only` below `sm` (accessible name unchanged): control bar **150 → 110px**, buttons in one row, textarea top **528 → 488** |
| QR: every code left the browser | ✅ | The generator FETCHED each code from `api.qrserver.com` — so a link, a phone number, a WiFi password went to a third party **on every keystroke**. Then `qr-code-styling` fixed the privacy but hardcoded six shapes in a private `switch`, so a dependency set the ceiling on how the tool could look. Now the only dep is `qrcode-generator` (it answers `isDark(row, col)`); the matrix, shapes, eyes, frames, export and logo compositing are ours. SVG/PNG/WEBP, and SVG is the source of truth |
| QR: preview was below the fold | ✅ | The code itself sat at **y=1671** — 2.3 screens down on a 720px viewport, under a wall of controls. Preview-first two-column layout, code sticky in the right rail at **y=361**; page **3,011 → 1,190px**. Five components deleted with approval (1,984 lines) |
| QR: quiet zone was illegal | ✅ | Measured **1.5 modules** against the **4** ISO/IEC 18004 requires — narrow enough that a code on a coloured background can fail to be *found*. Counted in modules now, not pixels, because module size shrinks as the payload grows. Verified `quietZoneInModules: 4` exactly |
| QR: shape catalogues are ours | ✅ | **16** module shapes / **9** eye frames / **12** eye centres (was 6/3/2), each guarded by a test: coverage **≥70%** of the cell or a reader thresholds it as light (`mosaic` is the floor at 70.6%), and distinctness measured **across four neighbour configurations** — `fluid`/`vertical`/`horizontal` are identical when isolated *by design*. Monkey's dotted eye **frames** deliberately refused: they break the `1:1:3:1:1` line the camera locks onto. Every swatch is drawn by the real path functions, and each group names what is selected |
| QR: style presets | ✅ | 16×9×12 is **1,728 combinations**, most bad, and a visitor making one poster cannot audition them — this is the one feature every paid competitor sells (saved design templates). 8 presets, each thumbnail a REAL code from the real renderer so the picture is a promise the click keeps. Sets shapes + ink only: the frame, caption, logo and quiet zone survive, because those carry the visitor's own content |
| QR: encode and paint were one memo | ✅ | Measured: encoding is **73%** of the cost (2.563 ms of 3.508 ms on a 250-char vCard) and depends only on the TEXT — so every frame of a colour drag re-encoded an unchanged symbol. Split: a style change is now **26%** of a keystroke (0.887 vs 3.409 ms), and the 8 thumbnails share **one** encode (1.9 ms cold, cached after) |
| QR: gradient could not be warned about | ✅ | `checkScannability` measured only the FIRST stop, so black → sky-blue passed silently while half the modules painted at **2.6:1**. Verdict is now the worst stop, and it separates "may fail in poor light" from "expect failure" (`severe`). Caught my own `brand` preset in the test — teal-700 at **5.4:1**, the brand hue itself at 2.6 — now 15.1/7.8 |
| QR: `<defs>` ids collided | ✅ | Found in the browser: the gradient id was `qr-gradient-${type}`, so the "Brend" thumbnail and the preview both emitted `qr-linear-…` — SVG ids are document-global. Id now derives from scope + type + both colours; verified `duplicateDomIds: []` |
| QR: the catalogue drew 14 of its 16 shapes | ✅ | Owner spotted it: "nuqta" and "juda yumaloq" looked the same. They **were** the same — a corner radius of half the cell IS a circle. Measured with the browser's own rasteriser (`isPointInFill`, 300×300): `extra-rounded` / `dots` differed by **0 cells of 4,421**, and `classy` / `leaf` by **32 of 5,023 (0.6%)**. `extra-rounded` → 0.4 radius (matching `eyes.ts`), `leaf` → the mirror diagonal, and the freed slot became a new silhouette family, `arch`. Closest surviving pair is now `rounded`/`sharp` at **1.75%** |
| QR: the distinctness test tested strings | ✅ | It passed on both duplicates above, because a circle built by `roundedRect` and one built by `circle()` are different TEXT. Rewritten to compare geometry: every shape is now declared once as a `ShapeSpec`, the path and the coverage figure are both derived from it, and pairs are scored by area + per-corner difference across four neighbour contexts. Threshold calibrated to the measurements (removed pair 1.08%, closest kept pair 3.36% on that metric) |
| QR: two radii tables that could disagree | ✅ | `modulePath` and `coverageOf` were parallel `switch`es holding the same numbers by hand — so the 70%-coverage safety test could validate a shape the renderer never drew. One table now; `mosaic` measured at **70.2%**, the floor |
| QR: the logo sat on live modules | ✅ | The footprint dropped `size * logoSize` modules while the image is sized against `extent`, which spans `size + quietZone*2`. Measured on a 21×21 code at quiet zone 8: the logo covered 8.14 modules, 7 were dropped, **5 painted modules were left underneath** — 21 at a 30% logo. Those are bits the error correction never budgeted for losing. Derived from the same geometry now, with a test across 6 quiet-zone/logo-size combinations |
| QR: locale switch wiped the visitor's work | ✅ | Reported as a reset. Diagnosed, not guessed: a marker on `window` **survived** the switch (`ctx-781836`) while the textarea came back empty — so it is a soft navigation that REMOUNTS the tree, and `useState("")` starts over. Draft moved to a module-scope Zustand store, deliberately **not** persisted (the payload can be a WiFi password, and storage would only add surviving a reload, which was never the complaint). Verified end to end: text, code, module count and the chosen preset all survive, and the UI is in English afterwards |
| QR: the code was 2.2 screens down on a phone | ✅ | The preview-first layout was desktop-only — stacked, the whole style panel came first, putting the code at **y=1805** on a 375×812 screen. Three explicitly placed grid cells now give input → CODE → controls on mobile and input-over-controls beside the code on desktop: **y=580, 0.71 screens**. It also fixed the sticky rail, which had **0px of travel** because `items-start` collapsed its cell — now 451px, verified pinned at `top: 80` from scrollY 200 to 500 |
| QR: three small a11y defects | ✅ | The code's `aria-label` was `frameLabel`, which defaults to "SCAN ME" and keeps its value when the frame is off — screen readers announced a caption that was not on screen. Every shape group carried an `sr-only <legend>` **and** a visible label, so its name was read twice; now `aria-labelledby` points at the visible one. Swatch tooltips showed raw ids (`extra-rounded`) beside their own translated names |
| Locale switch, the general case | ✅ | The QR fix applied where the same bug lived: **latin-cyrillic** (a pasted article, gone) and **`/tools`** (query + filters, gone). One module-scope store each, none persisted — the payload can be a WiFi password or somebody's API response, and the remount happens inside one JS context so storage would buy only surviving a reload, which nobody asked for |
| `/tools` re-animated like a refresh | ✅ | Owner's words, and measured: **26 `rise` animations replay** on a locale switch — heading, three filter rows, count and all 17 cards, ~1.3s. Entrance is now once per route per tab (`useRouteEntrance`), so changing language just swaps the text. The `typeof window` guard is load-bearing: the Set is process-global on the server, so consulting it there would let one visitor's arrival decide the next one's markup and hydration would mismatch |
| `/tools` search missed the obvious word | ✅ | Typing **"generator"** returned **0 results** in Uzbek — the titles say "Yaratuvchi". The slug is matched too now, so the word in the URL and in every developer's head finds 5 tools |
| Tool order had no rule | ✅ | Positions 3–6 were four developer tools in a row while general ones scattered to 7, 10, 11, 13. Reordered by a stated rule — proven traffic, then tools someone arrives looking FOR, then the once-only inspectors (screen size, IP, microphone). The JSON-LD `ItemList` is derived from the same array now: it claimed `numberOfItems: 15` against 17, listed 6, and put QR first while the page put latin-cyrillic first |
| Tool 3 of N: **JSON formatter** | ✅ | Chosen on cost×value: 557 lines and 30 hardcoded colours, the cheapest complete win, and the utility this site's books audience actually opens. Hardcoded colours **30 → 0**, `dark:` variants **13 → 0**, width `max-w-7xl` → `max-w-[1536px]` (it was 256px narrower than the header above it), the control card → a row, `ShimmerButton` forked with six `!important` overrides → plain `Button`, Formatted/Minified → the shared `SegmentedControl` |
| JSON: three `alert()` calls | ✅ | File rejection was `window.alert` carrying hardcoded Uzbek — while `Errors.fileTypeError`, `fileSizeError` and `fileReadError` sat translated and **unused in both bundles**. Now an inline `role="alert"`. Same story in `InfoSection`: it hardcoded `"name": "John", "city": "New York"` while the translated examples sat unused. Those need `t.raw` — a `{` opens an ICU argument, so a JSON snippet is a malformed message and threw `MALFORMED_ARGUMENT` |
| JSON: the error classifier was stale | ✅ | Found by testing the logic instead of the UI. It matched **old V8 wording**, so today's two commonest mistakes — `Expected ',' or '}' after property value` and `Expected ':' after property name` — matched nothing and both showed a flat "invalid JSON format". Fixed, and the check order is load-bearing (the colon message also contains the words "property name"). The tool now also reports **line and column**, which it never did |
| JSON: Tab indentation never worked | ✅ | `JSON_INDENTATION_OPTIONS` has offered Tab since the tool shipped; the code ran `parseInt("\t")` → `NaN`, which `JSON.stringify` reads as no indentation. Also: the footer divided `length` by 1024 and called it KB, so a Cyrillic payload was reported at roughly half its size — bytes now |
| JSON: collapsible tree view | ✅ | The view a formatter earns its keep with — fold the noise, open the branch you came for. Third segment next to Formatlangan/Siqilgan; nodes past depth 3 start folded so a huge response arrives as an overview; collapsed branches read `telefon: [2 element]`. Copy/download from tree view give the formatted text (a view, not a third serialisation). Verified in the browser: expand/collapse, guide lines, per-type colour |
| JSON: syntax colour onto tokens | ✅ | The tree AND `CodeHighlight`'s JSON branch now colour from `--chart-*` — the palette the design system reserves for data visualisation, which syntax colouring is. Replaces five hand-picked hues × two schemes in `packages/ui` that answered to nobody; dark mode now comes from the token, not from ten `dark:` twins. The HTML/CSS/JS branches of `CodeHighlight` still carry palette classes — same treatment, next pass |
| JSON: document stats | ✅ | `8 kalit · 3 pog'ona` next to the byte size — the QR page's "29×29 modul" move. One `measure()` walk in `analyseJson`, tested (keys/items/depth, scalar = 0, invalid = none) |
| Tool 4 of N: **password generator** | ✅ | Same architecture as the QR page by construction: the password is the hero card with its actions on it (it used to be printed into a read-only textarea labelled **"Tool Kirish"**, as if the visitor had typed it), settings under it, and a sticky rail of measured facts. Hardcoded colours **95 → 0**, `dark:` **22 → 0**, `max-w-7xl` → `max-w-[1536px]`. Deleted with the rebuild: `ConfigPanel.tsx` (343) and `StrengthPanel.tsx` (128), both consumed only here |
| Password: the strength meter now does arithmetic | ✅ | The old scorer graded the generated STRING (points for variety, minus for `123`) — right for auditing a human's password, wrong for a generator whose search space is known exactly. And its entropy readout hardcoded `95 ** length`: a 6-digit PIN was credited ~394 bits. Now `length × log2(alphabet)` from the real settings — PIN reads **20 bit · bir zumda · destructive meter**, 16 mixed reads **103 bit · asrlar**, and `memorable` is honestly graded weak (two words from a 32-entry list is ~23 bits, and saying so is the feature). Crack time quoted against a stated model: offline, 10^10 guesses/s, with the caveat printed under the numbers. 8 tests |
| Password: per-class character colouring | ✅ | Digits and symbols coloured apart from letters in the display (the 1Password convention — "is that l or 1?" answers itself). Masked mode shows plain dots ON PURPOSE: colouring the mask would leak which positions hold digits over a shoulder |
| Password: `t: any`, twice | ✅ | The hook took the TRANSLATOR as an untyped argument (two of them), rebuilt its five presets from it every render, carried hardcoded-Uzbek toasts wired to `console.log`, and held strength colour classes. Presets are constants now (`isPresetActive` drives the chip state), strings live in the bundles, and the draft store makes it the **4th** tool whose work survives a locale switch — verified: same password before and after |
| ⚠️ Password: download button NOT carried over | ⚠️ | The old UI offered "Yuklab olish" — the password plus all its settings written into a plaintext `.txt` with ~15 hardcoded-Uzbek lines. Saving passwords to plaintext files is the anti-pattern every manager warns about, so the rebuild left it out — but that is a FEATURE REMOVAL and needs the owner's yes. Restoring it (translated) is ~30 lines if wanted |
| Segmented control: the label blink | ✅ | Owner: the tab's caption "swaps weirdly" mid-slide. Diagnosed as TWO CLOCKS: the bar slides in 300ms, the label colour transitioned in 200ms — so the new label turned light while the dark bar was still 100px away (light-on-light ≈ invisible), and the old one turned muted while still ON the bar. Fixed architecturally, not with a library: a light copy of the whole label row lives INSIDE the bar, clipped by it and counter-translated (+L / −L, one timing) — the reveal is pixel-locked to the motion by construction, the iOS way. framer-motion would not have helped: it animates values; the fix is one clock instead of two. Verified: bar over active option **dLeft 0 / dWidth 0**, copy over base label **0 / 0**, transforms exactly opposite (+120.43 / −120.43), both layers `duration-300` |
| Segmented control: `left-1` is load-bearing | ✅ | The reveal needs the bar painted OVER the labels, i.e. rendered after them — and an absolutely-positioned last child takes its static position past the last label, not at the origin the measurement assumes. Pinned to the track's content origin; re-verified on the original consumer (latin-cyrillic): at rest and after a direction switch, 0/0 |
| JSON tree: optical centring + hover pill | ✅ | Owner's zoomed screenshot: the chevron sat high against the brace. Geometrically it was centred (measured 0.1px) — the offence is OPTICAL: a monospace brace draws its weight below its box centre. `top-px` nudge on the chevron, and the hover pill got air (`px-1.5 py-0.5` mirrored by negative margins so the text stays on the tree's grid) |
| Suite consistency pass | ✅ | Audited all four refactored tools against one rule set. Aligned: page width `max-w-[1536px]` ×4 · `ToolHeader` ×4 · kicker-square panel headers · `SegmentedControl` for every mode switch · sticky right rail on QR/password · explicit 3-cell grid for mobile order ×2. **Fixed:** JSON had TWO clear controls (bar "Tozalash" + the panel X every other tool uses) — bar button removed, the rule is "clear sits on the panel it clears"; "Namuna JSON" → "Namuna" (the page already says JSON three times). **Deliberately different, documented:** QR has no control bar (the style panel IS the controls), sample lives in Latin's empty state but JSON's bar (a converter starts from your text; a formatter demos a format) |
| Homepage design review (owner-requested) | ✅ | Full audit of the hero in BOTH schemes, measured. Verdict: the foundation holds — 9 animation families all running (`grid-drift`, `rain-fall` ×3, `aurora` ×2, `accent-pan`, `rise` ×35, draw-in), every one behind `prefers-reduced-motion`, **zero** hover-states without a transition, terminal palette typing loop live in both modes. The owner's dark-mode instinct was CORRECT and measured: the outline CTA's `bg-background/60` wash is the page's own colour in dark, so the pill sat at **1.0:1 against the page** — a hole with a 34% border next to a crisp white primary. Fixed with the one token that is "one step off the page" in both schemes: `bg-card/60` (1.04:1 light / 1.11:1 dark — the ~5% lift Linear/Vercel give dark secondary buttons). Verified after reload in both modes; light unchanged. Also: measuring mid-`transition-colors` gives MIXED token readings — steady-state only, and `html` can carry BOTH `light dark` classes when next-themes restores over a manual class |
| Hero terminal: three naturalness fixes | ✅ | The typing loop's tells, each measured then fixed in CSS alone. (1) **Metronome typing**: one `steps(5)` = five keystrokes at exactly 180ms each — a rhythm no hand produces. Now five hand-placed keyframes at 115/168/105/252/262ms gaps (burst, hesitation, finish), `step-end` so characters land discretely; deletion stays even and fast because key repeat IS a metronome. Verified by scrubbing the animation: widths land at 115/284/389/641/903ms. (2) **Results preceded the query**: the crossfade started at 0%, so answers were ~70% visible before typing finished. Held at 0 until 4.5%, then a 0.6s fade — the debounce every real search has, played back. Verified: opacity 0 at 500ms, 1 at 1800ms. (3) **Caret blinked through the typing**: real carets reset their blink timer on every keystroke. New 7s track (21s ÷ 3 identical thirds): solid through typing, four soft breaths while reading, solid through delete. Verified: opacity 1.0 mid-typing |
| Hero caret sat high — and WHY the first fix failed | ✅ | Owner: the caret floats above the icon and text. Measured: centre 2.7px above the icon's, bottom 5.5px short of the descender zone. First fix adjusted `vertical-align` — **no effect, and the lesson is the row**: the caret is a FLEX ITEM inside `items-baseline`, where `vertical-align` does nothing; baseline alignment glues an empty inline-block's BOTTOM to the text baseline, hanging the whole bar high. `align-self: center` + `height: 1em`: caret centre now **0.0px** off the icon's and the text's |
| Chapter-card hairline: added, rejected, reverted | ✅ | I added a mini-divider (index + hairline) to the homepage chapter cards as "premium structure". The owner rejected it on sight — bachkana. Reverted fully, recorded in memory: card interiors stay plain; structure comes from type hierarchy, polish means measured precision, not decoration |
| /tools: a directory that showed no tools | ✅ | Owner disliked the filter section, and the measurement agreed with them: centred header + two filter groups EACH WITH A HEADING + centred search + a standalone count row put the first card **~940px down** — the tools page showed everything except tools. Rebuilt as one toolbar row (search left · category chips · audience switch right), header left-aligned onto the site's gutter like every other page. Measured after: first card at **354px**, **8 cards on the first screen** (was 0), mobile 375px no overflow, dark/light both verified |
| /tools: one pill species too many | ✅ | The audience filter was a third kind of chip. It is now the suite's shared `SegmentedControl` — same control every tool page uses for its modes, sliding reveal included (verified 0/0 alignment). The audience BADGE left the card corner too: "Umumiy" on ten of seventeen cards was a corner spent on the least differentiating fact — category and audience now share the card's one mono footer line, `Generatorlar · Umumiy` |
| /tools: the count line and card density | ✅ | Owner: the count sat glued under the search box. Root judgment: at rest it DUPLICATED the "17" already printed in the all-tools chip — so it now renders `sr-only` at rest (still in the a11y tree, so `aria-live` keeps announcing filter changes) and appears only when a filter or query narrows the list; verified "6 ta vosita topildi" on the Generatorlar chip. Cards: icon moved INLINE with the title (the row a 40px icon used to own is gone) — **230 → 150px per card, 12 tools on the first screen**. Sidebar-directory and list/grid-toggle variants considered and declined: a 17-item catalogue does not carry a 280px persistent sidebar (⌘K already reaches every tool from anywhere), and a view toggle earns its keep only when items have comparable attributes to spread into columns |
| /tools: four follow-ups from the owner | ✅ | (1) **Segmented label colour "swaps instantly"** — this is the two-clock bug already rebuilt this session; proved END-TO-END by scrubbing the live CSSTransitions to t=150ms: bar at **+60.5px**, clipped copy at **−60.5px**, lockstep, three 300ms transitions — the colour sweep IS the bar. The sighting was the pre-rewrite bundle. (2) **Cards swapped instantly on filter after a revisit** — `grid-rise` was gated on the once-per-tab entrance; a `hasInteracted` ref now replays the stagger for every filter/search interaction while locale-switch remounts still stay quiet. (3) **Filters now live in the URL** — `?q/cat/aud` via `history.replaceState` (a router navigation per keystroke would schedule work nobody asked for), hydrated once in an effect, unknown values fall back, defaults keep the URL clean. Verified: `?cat=generators` refresh → 6 cards + active chip; reset → clean. (4) **Active chip was barely-on in light mode** — `/40` border → full-strength chart-token border + `/12→/15` wash + `font-medium`, all four categories + neutral |
| QR: first integration tests in `src/` | ✅ | `testing-strategy.md` calls integration the fat layer and the whole tree had none. **11 RTL tests** driving the real UI — type, preset, clear, remount, contrast warning — plus 84 unit tests. Suite **473 → 509**. Needed two `vitest.config.ts` lines: next-intl's client barrel imports `next/navigation` without an extension, which Node's ESM resolver rejects, so no component using `useTranslations` could be rendered in a test at all |
| latin-cyrillic deep audit: Escape in the dialog wiped the text | ✅ | Portaled overlays live outside the tool's DOM but INSIDE its React tree, so their keys bubbled synthetically to the tool's key handler: Escape in the exceptions dialog's input ran `clearAll()` — the visitor's article, gone — and its `preventDefault` could suppress Radix's own close. Fixed with a DOM-containment guard (`event.currentTarget.contains(event.target)`), verified live pre/post, pinned by a regression test |
| latin-cyrillic: filename chip outlived the file | ✅ | Import `hujjat.txt`, then edit or hit "Namunani sinash" — the chip kept naming a file the panel no longer held. Manual edits through `onSourceChange` now drop it; imports call `setSourceText` directly so the chip survives the import that created it. Also: stale `cn` import removed |
| BaseModal rebuilt on the product's overlay idiom | ✅ | Was framer-motion `AnimatePresence` wrapped AROUND the Radix portal — two presence managers over one subtree — with `onOpenChange={onClose}` (fires on `true` too) and no Radix `Title`/`Description` (dialog had no accessible name, console warning per open). Now Radix data-state + tw-animate CSS, exactly like dropdown-menu/select; `ExceptionsDialog` uses `BaseModalTitle/Description`. Only real consumer (grep: ExceptionsDialog; CodeBlock mentions it in a comment only). Honest note: the "zombie dialog" seen mid-audit was the hidden-pane animation freeze, an artifact of the measuring environment — verified gone with the pane visible (0 dialogs, `overflow` restored) |
| ProgressBar: a second job pinned the bar at the old maximum | ✅ | The monotonic clamp (right for in-job reports) ate the reset when a NEW import started during the completion hold or mid-read: target fell to 0, `Math.max` held `current` at ~100 for the whole job. In-job reports never decrease, so a falling target now IS the new-job signal → reset to 0 inside the rAF loop |
| transliterationStore migrate rebuilt field-by-field | ✅ | v1 records hold `direction`, not `preference`; the spread migrate left `preference: undefined` for every visitor migrating from v1 — no active segment, engine handed a value outside its three. Now validates against the three legal values, falls back `"auto"`, and `exceptions` must be an array |
| Extension: popover wore the old brand | ✅ | `content.ts`'s shadow styles were still the sky palette while popup + site moved to teal 217° — one extension, two brands. All popover colours now come from a `TOKENS` table copied from `src/styles/tokens.css` (same copy-with-note pattern as the popup's `style.css`; the shared-token-file refactor stays tracked under Next up). Popup's leftover `sky-*`/raw-success classes → token classes (`bg-primary`, `text-success` + new `--success` tokens). Gradients → flat primary. Verified in the built output: 0 sky hits, token utilities generated, `wxt build` exit 0 |
| Extension: three functional gaps | ✅ | (1) Scrolling INSIDE the popover's own textarea closed the popover — the capture-phase document scroll listener fired on shadow-retargeted events; now ignores scrolls born in the host. (2) Context menu + ⌘⇧L acted invisibly on read-only pages (clipboard write, zero feedback) — a small shadow-DOM toast now says "Almashtirildi"/"Nusxalandi"; Escape also dismisses the popover. (3) The popup footer showed `⌘+Shift+L` to Windows/Linux users whose real binding is `Ctrl+Shift+L` — platform-detected label |
| latin-cyrillic: integration tests, per the module's own priority list | ✅ | **15 RTL tests** driving the rendered UI: auto-direction both ways + label flips, forced direction, swap pinning the opposite, sample, Escape-clears vs **Escape-in-dialog-must-not-clear** (the regression above), exceptions add/duplicate/preserved-in-result, Cmd+Enter copy ack, locale-remount draft survival, dropped-.txt import with CRLF/blank-line normalisation, chip dropped on edit, unsupported-type rejection. Suite **543 → 558**. jsdom stubs for `matchMedia`/`ResizeObserver` added to `vitest.setup.ts` |
| Found while gating: a suppression that suppressed nothing | ✅ | `global-error.tsx` had `biome-ignore` one line above an attribute-anchored diagnostic — Biome flagged it "no effect" and the error underneath surfaced as the gate's one red. Comment moved inside the opening tag, directly above the attribute |
| QR deep audit: machine-decode proof of the whole catalogue | ✅ | jsQR driven against the LIVE preview (serialize → canvas → decode) for every option: **16/16 module shapes, 9/9 eye frames, 8/8 presets after the fix below** — including with a logo (level H absorbed the footprint, 25→33 modules, still decodes) and a frame. The 45°-dense-vCard failure reproduced on plain squares too — decoder-bench limit, not a style defect |
| QR: split eye centres could not be FOUND by a scanner | ✅ | `bars-v`/`bars-h`/`dot-grid` all failed jsQR outright — the finder's 1:1:3:1:1 ratio includes the 3-module dark CENTRE, which the file's own comment wrongly claimed was free to split. The **"tech" and "brand" presets shipped broken codes.** Fixes: dot-grid rebuilt as four dots fused across the centre lines (lobe radius 0.25·√2 — the minimum that keeps the crosshair solid; decodes at 0°/90°/45°), bars given a perpendicular spine (decode at aligned rotation only — **still rotation-fragile, removal is an open owner decision**), tech preset moved to bevel. The corrected law is now documented in `eyes.ts` |
| QR: gradient painted each eye its own rainbow | ✅ | `objectBoundingBox` units on a gradient shared by four separate paths gave EVERY eye its own full from→to sweep while the data path swept the whole code once. Both the preview and `modelToSvg` now emit `userSpaceOnUse` over the full extent — verified live (`x1=0 x2=320`, bottom eye now mid-sweep teal), and the two renderers stay pixel-identical by construction |
| QR: "add gradient" defaulted to a colour the tool itself condemns | ✅ | `DEFAULT_GRADIENT_COLOR` was the interface teal at 2.6:1 — one click produced the severe "will not scan" warning about the tool's own choice. Now the Brend preset's measured 7.8:1 stop; verified live: no warning |
| QR route: structured data described a tool that does not exist | ✅ | FAQPage schema promised "PNG, JPG" (real: SVG/PNG/WebP, no JPG anywhere), a WiFi form the page deliberately does not have, and a featureList advertising the REMOVED size/error-level controls. All three aligned with reality in both locales' schema strings. The page still carries the old-style inline metadata blob (RU keywords etc.) — the latin-cyrillic-style `seo/` segment is the tracked follow-up |

| JSON deep audit: the tree view could not carry a real document | ✅ | Measured on a 384 KB, 5,000-item API-shaped payload: **paste blocked the caret for 878 ms, every keystroke 94 ms** (parse + two stringifies + stats ran synchronously per keystroke), and switching to tree view took **3.95 s and put 180,000 nodes in the DOM** — depth-based folding was no guard against a WIDE branch. Fixes: branches >50 children start folded at any depth, children reveal in slices of 100 ("Yana 100 ta ko'rsatish (4900 ta qoldi)", both locales), analysis debounced 150 ms behind the live textarea (`hasAnalysis` gates the badges so fresh input is never judged by a stale result), syntax highlight falls back to plain monospace above 300 KB. Re-measured: paste 22 ms, keystroke 16 ms, tree DOM 464 nodes. 3 RTL tests pin the fold/chunk behaviour |
| JSON: two small exactness fixes | ✅ | Tree strings render via `JSON.stringify` (a value holding `"` or a newline used to display unescaped — the tree is a view of the document, not a paraphrase); `formatBytes` gained an MB tier (a 10 MB file read "10240 KB") |
| Password deep audit: the core survives 120 live draws | ✅ | Driven through the real UI: 120/120 unique, **0 violations of the strong-type "one of each class" guarantee**, 88 distinct characters, frequency spread 11–35 around an expected 21.8 — CSPRNG + rejection sampling behaving. The no-classes error and preset strip verified |
| Password: memorable draws could silently lose their entropy | ✅ | Words were picked blind and the overflow sliced: at length 16 the live page produced **"ProtocolPassword"** — two dictionary words, the number and symbol (13 of the format's ~24 bits) truncated away while the meter reported the full floor — plus "CryptoCrypto682&". Now the number and symbol are budgeted first and the words chosen to fit around them, doubled words excluded; 30/30 live draws keep every part. 2 regression tests |
| Password: two acknowledgement fixes | ✅ | "Nusxalandi" no longer outlives the password it described (reset on regenerate); a refused clipboard write no longer claims success (try/catch, no ack) |

| QR: the WiFi form — the one format that earned fields | ✅ | Owner approved. Two modes, not five: URL/text/SMS stay in the free box; WiFi alone gets SSID + security (WPA/WEP/open) + password + hidden-network, compiled by `utils/wifi.ts` with the reserved-character escaping (`\ ; , : "`) nobody hand-writes correctly. Same pipeline, so the badge/contrast/export know nothing about modes; draft (incl. the password) lives only in the module-scope store. Proof: the live form's QR decodes to the exact escaped payload (`…P:juda\;maxfiy\:parol;;` via jsQR). 5 unit + 4 RTL tests; route FAQ updated to describe the form |
| ColorConverter (5th tool) rebuilt onto the suite | ✅ | The audit found: `onSuccess`/`onError` fired INSIDE `useMemo` (render-phase side effects wired to console.log), history written on every keystroke that parsed ("#ff0" recorded while typing "#ff0000"), a private 12-entry colour dictionary shadowing the 195-entry `color-names.ts` registry, four layers of ambient-glow divs, `GradientTabs`/`ShimmerButton`/`TerminalInput` chrome, `t() \|\| "fallback"` throughout, all-default exports, `max-w-7xl`. Now: suite cards + SegmentedControl + tokens, module-scope draft store (locale remount), recording only on DELIBERATE picks + blur, shared `CopySwatch` (3 consumers), the history panel refreshes via a `historyVersion` signal (it used to read storage on mount only — a colour chosen five seconds ago never appeared), gradient's fake "Tailwind class" export (everything mapped to grey) replaced with the arbitrary-value form. Verified live: oklch input, palette 11, analogous switch, history updates on pick |
| Consistency sweep over the remaining 12 tools | ✅ | Base64/JWT/URL/Hash/UUID/OgMeta/Lorem/DeviceInfo/Screen/IP/Camera/Mic: **620 hardcoded palette hits → semantic tokens** (exact light+dark pair mappings only; ~270 context-dependent hits deliberately left for per-tool passes — baseline locked), `max-w-7xl → max-w-[1536px]` on all 12, debug `console.log` noise stripped from 13 files. Light+dark spot-checked. HONEST SCOPE: this was consistency, not flagship rebuilds — `TerminalInput` traffic-light headers, `GradientTabs`, JWT's blocking `alert()`s and per-tool UX are still the old generation |
| url-encoder: the standing i18n red, closed the right way | ✅ | The 8 "dead" en-only keys were not dead — the UI hardcoded their VALUES ("Protocol:", "hello world & symbols") next to unused keys. Added the 8 uz translations AND wired the labels/examples to `t()`, so the keys are used and both locales ship. **`pnpm i18n` exits 0 for the first time this branch** — no deletion needed, so nothing was waiting on approval after all |
| Found in passing: device-info burned CPU on every visit | ✅ | "Maximum update depth exceeded" in a loop: an effect depended on `deviceInfo` while calling `detectDevice()`, which wrote a fresh object into it — detect → new state → re-run → detect. Split: detection runs once; the online/orientation listeners subscribe once and use functional updates. Verified live: 0 loop errors in 2 s post-fix. Also: a `biome --write --unsafe` pass silently stripped two deliberate signal-dependencies (`[password]`, `[historyVersion]`) and added a use-before-declaration in ScreenResolution — caught, restored with explanatory suppressions; unsafe autofixes are off the menu |

| ColorConverter deep audit: the OKLab it published was a different colour space | ✅ | `rgbToOklab` described itself as a "simplified approximation" and was in fact invented maths — `L` was the WCAG luminance of the GAMMA-ENCODED channels, `a`/`b` were channel differences — while `oklchToOklab` divided by 100 with no counterpart in `oklabToOklch`, so the pair could not round-trip. Measured on the site's own `#0d5a6b`: the tool said `oklch(0.29 0.19 216)`, the truth (hand-computed, then pinned) is `oklch(0.433 0.073 217)` — **chroma out by 2.6×**. Since Tailwind v4 and this repo's `tokens.css` are written in OKLCH, the tool was handing developers numbers that name a different colour. Replaced with Ottosson's published matrices — the same constants `scripts/contrast-check.mjs` already gates the design tokens with — and rounding moved out of the maths into the display layer (rounding `a`/`b` first moved the hue 217.5° → 217.8°). **9 new unit tests** against the published sRGB primaries |
| ColorConverter: the parser rejected the syntax devtools copies | ✅ | `rgb(255 0 0)`, `rgb(255 0 0 / 50%)`, `hsl(200deg 50% 40%)`, `100%` channels, `transparent` — every CSS Color 4 form a modern devtool, Figma export or Tailwind v4 token emits — reported "Noto'g'ri format". Rewritten around one functional-notation reader; channels are now clamped at parse (`rgb(300, 0, 0)` used to reach `rgbToHex` unclamped). **13 new unit tests** |
| ColorConverter: WCAG contrast, the question a colour tool must answer | ✅ | New panel: the verdict first ("oq matn eng yaxshi o'qiladi: 7.80:1"), then the reading against white and against black with AA / AAA / AA-large ✓✗ per backdrop and a live sample painted on literal white and black (a preview on `bg-card` would show a ratio nobody measured). Alpha is composited onto each backdrop before measuring — `transparent` correctly reads 1.00:1 on both, not 21:1. Grading runs on the raw ratio, not the rounded one. The luminance maths was promoted to `lib/utils/color-contrast.ts` at its **second** consumer; `QrGenerator/utils/contrast.ts` keeps its own scanner verdict and imports the maths |
| ColorConverter: a grey came back tinted red | ✅ | `generateTailwindShades` forced saturation floors (`max(s-20, 10)`, `min(s+10, 100)`) onto colours that had none, so `#808080` produced `#f8f7f7 … #2a2222` — a red ramp from a pure grey — and `generatePalette`'s split-complements used `max(s-20, 40)`, which raised saturation ABOVE the base for anything under 40. Both now move down from the base, never past zero. Measured after: every shade of `#808080` has channel spread **0** |
| ColorConverter: five identical swatches, four React key collisions | ✅ | Rotating the hue of an achromatic colour is a no-op, so `analogous` on `#808080` returned the same grey five times — keyed on an identical hex, producing four "same key" warnings per render (captured live). A palette is a set: `generatePalette` de-duplicates. The grid also stopped sizing every palette for the largest — the three types return 11, 5 and 10 colours and `lg:grid-cols-11` rendered the analogous five with six empty columns; `auto-fill` now fits whatever arrives |
| ColorConverter: the favourite heart lied about storage | ✅ | `isColorFavorite()` — a `getItem` plus a `JSON.parse` — was called four times per swatch inside the JSX. With the React Compiler on, that memoised: measured, un-favouriting removed the colour from `localStorage` while the icon kept reporting `aria-pressed="true"`. Reading mutable globals during render was the defect; the read count was the symptom. Now derived from a `Set` built from React state. Verified live: false → true → false, storage in lockstep |
| ColorConverter: three more shipped defects | ✅ | (1) One stray character flipped `<input type="color">` to `#000000` and it stayed there (the DOM coerces anything not a 6-digit hex) — the picker now holds the last valid colour while the text is mid-edit. (2) The opacity slider rendered only when the colour was ALREADY translucent, so there was no way in from an opaque colour except hand-writing `rgba(…)` — it is always present and writes an 8-digit hex. (3) History stored the raw input, and `addToColorHistory` upper-cases it, so typing `teal` filed an entry literally reading `TEAL`; it stores the canonical hex now |
| ColorConverter: the Tailwind exports were wrong in two ways | ✅ | The gradient's Tailwind class emitted `bg-linear-to-r` whatever the type — choose "radial", copy, and the destination got a LINEAR gradient — and any stop between the first and last was dropped in silence. Now: the idiomatic form for a 2–3 stop cardinal linear gradient (`via-` included), and the exact arbitrary-value form for everything else. The shade scale's "Tailwind Config" emitted a v3 nested config object, which has not configured Tailwind since v4 shipped — and this repo is on v4; it emits `@theme` now. Both builders moved to pure functions in `utils/exports.ts`; **12 unit tests** |
| ColorConverter: eyedropper, structure, and a 6-consumer card | ✅ | `EyeDropper` (Chromium-only, feature-detected so the button never appears where it cannot work) picks any colour on screen. Module brought onto the documented structure: `types/`, `utils/` (pure + tested), `constants/`, seo/. `ToolCard` extracted to `src/components/shared/` at its **sixth** in-tool consumer (13 hand-copies exist across the suite) — byte-identical markup, plus the heading now NAMES the region, which the hand-rolled `<section>`s never did. The format labels lost their chart-token rainbow (nine differently coloured names encoding nothing) and dropped to two lines: nine three-line rows measured 984px against a 657px column and needed an inner scrollbar |
| ColorConverter route: metadata that described another tool | ✅ | The inline blob advertised CMYK conversion and a triadic palette (neither has ever existed), carried a Russian keyword block for a locale the site does not serve, `softwareVersion: "2.0"`, and served the Uzbek title to `/en`. Rewritten as a `seo/` segment mirroring latin-cyrillic. The four FAQ questions were published to Google and rendered NOWHERE — a Search guidelines violation — so they now render (`ColorFaq`, shared `FaqAccordion`) from the SAME keys the schema reads; verified live that all four schema questions appear in the document |
| Found in passing: `/en` rendered the Uzbek bundle | ✅ | `LocaleMessages` documents "the locale from params — PASS IT", and this route did not: the English page shipped an English `<title>` over an Uzbek `<h1>`. One line. Also fixed here: the route imported through `@/modules/tools`, whose barrel is 21 `'use client'` modules — measured in the client reference manifest, **20 tool modules → 1** (every other tool route still ships all 20) |
| Found in passing: 46px of horizontal scroll on a phone | ✅ | At 375px the whole document scrolled sideways and every card rendered 406px wide. Two causes: the top grid had no explicit mobile track, so its single implicit column sized to content instead of the container (`grid-cols-1` spells `minmax(0, 1fr)` and caps it), and the palette-type control's three names come to 362px against a 343px content box — it scrolls in its own box now. Measured after: overflow **0px**, all seven cards exactly 343px |
| Found in passing: two stale CMYK claims and a missing CSS colour | ✅ | The site SEARCH index still described the tool as converting CMYK (`api/search/documents`), so the claim outlived the route metadata that carried it — rewritten to the formats that exist. And `color-names.ts` held 147 of the 148 CSS named colours: `rebeccapurple` — legal CSS since 2014 — was rejected as an invalid format. Verified by diffing the registry against the full CSS set |
| Found in passing: two suppressions that suppressed nothing | ✅ | Biome ignores a `biome-ignore` with other comment lines between it and the diagnostic — so the multi-line explanations in `ColorHistory` and `usePasswordGenerator` had no effect and the lint underneath was live. Both rewritten as one suppression line with the prose above it |

| Owner report: history swatches broke out of their card | ✅ | Reproduced with the owner's own data — 20 legacy entries, the cap. `CopySwatch`'s root `<button>` had no width constraint, and `truncate` clips only the caption's own box, so a long caption sized the BUTTON: measured cell 67px, button **144px**, swatches painting over each other and out through the card's right edge into 24px of document overflow. `w-full min-w-0` makes the cell the authority. The data behind it was fixed too: entries recorded before canonicalisation shipped hold the raw input (`addToColorHistory` upper-cases whatever it gets, so a typed `rgba(59, 130, 246, 0.6)` is stored literally as `RGBA(59, 130, 246, 0.6)`), and `color-storage.ts` now canonicalises on READ as well as write — every existing device is repaired without a migration, and two notations of one colour collapse to one swatch |
| Owner report: a pale line down the left of every gradient | ✅ | `--border` is a translucent WHITE in dark mode and `background-clip` defaults to `border-box`, so the colour paints UNDER the border and the border composites on top. Measured on the first stop: `rgb(13,90,107)` renders as `rgb(42,110,125)` at the edge — **+29/+20/+18**. `bg-clip-padding` on every surface that carries a colour inside a token border (gradient preview, `CopySwatch`, presets, contrast tiles, the alpha checkerboard). One trap on the way: the inline `background:` SHORTHAND resets `background-clip`, so the class had no effect until the style became `backgroundImage` — verified `padding-box` afterwards. In a colour tool a border that tints the colour is a correctness bug, not a cosmetic one |
| Found while reproducing: the shade ramp was not monotonic | ✅ | The curve was read as ABSOLUTE lightness with floors (`600 = max(l - 15, 35)`, `700 = max(l - 25, 25)`), so for any base darker than the floor the later steps were LIFTED above it. On the site's own `#0d5a6b` (L 24%) the scale ran 500 `#0d5b6d` → 600 `#14859f` → 700 — each **brighter** than the step before, visible in the owner's screenshot. Rewritten as anchored interpolation: 500 IS the base, the light half stretches to 97%, the dark half up from 10%, plus a strict-ramp pass so no two steps can round onto each other. 500 also returns the input hex verbatim now — the HSL round trip cost a unit per channel and handed back `#3c83f6` for `#3b82f6`. Strictly decreasing and 11 distinct steps for every base tested; pure white/black documented as the one case that physically cannot be strict |
| Found while reproducing: two cards, one dataset | ✅ | `generatePalette('monochromatic')` was literally `generateTailwindShades(...).map(s => s.hex)`, so on the tool's DEFAULT setting the "Monoxromatik palette" card and the "Rang soyalari (50–950)" card below it rendered **the same eleven colours** — the single biggest reason the page reads as padded. A scheme and a token ramp are different artefacts: monochromatic is now five steps on one hue with saturation easing at both ends. Also: both grids now take one track per colour on desktop instead of `auto-fill`, which had five swatches sitting in the first five of fifteen tracks with two thirds of the card empty beside them |
| Found while diagnosing the owner's failing commit | ✅ | `git commit` was being rejected by the lefthook **pre-commit** job `pnpm tokens`, not by commitlint: `color-names.ts` +1 (the `rebeccapurple` entry — colour data) and `color-palettes.ts` +6 (hexes quoted inside the evidence comments above). Re-locked with the documented `--update --force` escape; baseline 1466 → 1473, `measuredAt` 2026-08-03. Both pre-commit jobs and all three pre-push jobs verified exit 0 |
| Competitive research: 4 sweeps, 40+ tools | ✅ | Ran as a background workflow; the four research agents completed before the run was stopped, so the findings are salvaged from the journal while the design/judge phases are not. Verified facts that change our roadmap: our page measures **3,801px (4.8 screens)** against the QR generator's 2,997px, with **9 top-level sections against 3**; huetone fits in 720px with ZERO sticky elements and oklch.com pins `<main>`; uicolors reduces four whole sections to four toolbar words (Contrast matrix · Color info · Export · Edit); every scale generator surveyed offers a colour-NOTATION switch on the export (hex/OKLCH/HSL/RGB) and **shows the snippet before you copy it** — ours copies blind; every one names the palette and threads that name through the export, while ours hardcodes `--color-primary-*`; and Tailwind v4's own palette is authored in OKLCH, so a v4 `@theme` block full of hex already looks dated. Anti-patterns confirmed as ours: two cards showing one dataset (fixed above) and full-width prose explainers sitting between the result and the export |

| Workflow round 2: design panel, adversarial judges, two critics | ✅ | 19 agents, 0 errors. Three independent design angles converged on ONE architecture — pin the colour's VALUES on the right (QrGenerator's `row-span` + `self-stretch` + `sticky top-20` recipe), collapse the four stacked generator cards into one card behind a four-option SegmentedControl, one reference block below. The judge panel was hostile: **would-ship 2/9**, average findability 5.8/10. Reframe worth keeping: the page is **not too long, it is too FLAT** — nine bordered cards of identical weight, so nothing says "this is the point and that is the depth"; the QR page feels short because it has THREE weights, not because it is 800px shorter. The number to move is mobile: at 375x812 the RGB row sits at **y≈1,465**, 1.8 screens down. **Two verified defects the audit had missed:** (1) `getColorName` is an exact-hex Map lookup, so it returns `""` for essentially every real input — the colour-name line is dead in three call sites; (2) FAQ ANSWERS are absent from the HTML because Radix unmounts closed accordion content, so the `FAQPage` schema's four `acceptedAnswer.text` values have no on-page counterpart. My own earlier check only compared the QUESTIONS. **And the critics caught the fix:** `forceMount` alone is verifiably wrong — read in `@radix-ui/react-collapsible@1.1.20/dist/index.mjs`, `isOpen = context.open \|\| isPresent` (:97) with `isPresent` seeded from `forceMount` makes `hidden: !isOpen` (:129) false, so every answer would render permanently OPEN on two pages. Native `<details>` is the correct shape for an SEO FAQ. Full plan, cut list and rejections in the task output; four deletions need owner sign-off before any code |

| ColorConverter restructured: three weights instead of nine cards | ✅ | Owner approved the four deletions. Shipped: the toolbar row deleted (it governed cards ~900px below it); `ColorInputPanel` split so the card the visitor AUTHORS in holds no identity block; a pinned `ColorSummary` on the right carrying the swatch, the contrast verdict sentence, four primary format rows and a `<details>` for OKLab/LCH/Lab — the QrGenerator `row-span` + `self-stretch` + `sticky top-20` recipe verbatim; four full-width generator cards collapsed into one `Workbench` behind a four-option SegmentedControl with every panel MOUNTED and the inactive ones `hidden`, so the route's `featureList` stays true of the served HTML. RGBA/HSLA rows deleted — RGB and HSL are alpha-aware in the CSS Color 4 slash form, so all seven rows are one syntax family instead of two. **Measured, 1280×800: 3,801 → 3,241px, sections 7 → 4, and the pinned card is 641px against 720px available — it fits with no nested scroller, which was the critics' single biggest risk.** Phone 375×812: 6,517 → 5,765px and the RGB row 1,465 → 1,006px. **Honest miss:** the stated target was the RGB row above the fold and it is not — clearing 812px would mean cutting the shared `ToolHeader`, which is a suite-wide decision, not this tool's |
| ColorConverter: five things the restructure was really for | ✅ | (1) **Export is a panel, not three blind buttons** — target × notation (HEX/OKLCH) × an editable token name, with the snippet rendered in a `<pre>` before you copy it; the three builders emitted hex only and hardcoded `--color-primary-*`, so two exported scales collided. (2) **`getColorName` was dead** — an exact-hex Map lookup returning `""` for essentially every real input across three call sites; now nearest-by-OKLab over the 139 unique registry hexes with a perceptual ceiling, and the export's token name is derived from it. (3) **The nearest passing shade**, click-to-adopt, when the colour fails AA on both backdrops — guarded to opaque colours, because the ramp is built from the opaque hex and suggesting a shade to fix a translucent colour is advice about a different colour (measured: `transparent` was offered a 21.00:1 "repair"). (4) **SC 1.4.11 surfaced** — `gradeContrast` always returned `nonText` and the panel discarded it; large text is now also rendered at 24px, the size it is graded at. (5) `hexContrast` silently dropped alpha for an 8-digit hex, so the optional third backdrop ignored transparency while the two beside it composited it |
| ColorConverter: the FAQ finally reaches the HTML | ✅ | The `FAQPage` schema had been publishing four `acceptedAnswer.text` values with no on-page counterpart, because Radix unmounts closed collapsible content and nothing opened by default — and `forceMount` is verifiably NOT the fix (`isOpen = context.open \|\| isPresent` at `@radix-ui/react-collapsible@1.1.20/dist/index.mjs:97` would render every answer permanently open on every FAQ on the site). Rewritten as native `<details>`: always in the document, collapsed by default, zero JavaScript, a Server Component with no client leaf. Verified in the DOM. The two prose cards became one server-rendered `FormatReference` table covering all SEVEN formats — the deleted cards covered three — with per-format Uzbek prose and a worked `FF → 255` example. `?c=<hex>` URL state via `replaceState` makes a colour shareable; the active view deliberately stays out of the URL |
| ColorConverter self-review: the swatch teleported out from under the pointer | ✅ | Owner: "the first tile always shows a checked mark, like a flicker". Reproduced and measured, not guessed — clicking a saved swatch at **x=418** left it at **x=53** by the time the click finished, because selecting re-recorded the colour and `addToColorHistory` moves an existing entry to the FRONT. React keys the grid by hex, so the node was MOVED, and a browser does not re-evaluate `:hover` until the pointer moves again: `:hover` still matched the moved node (`badgeOpacity: 1`) and the badge went out on the next twitch. Two fixes, one for the cause and one for the class: taking a colour back out of the saved list is a READ and no longer re-records (grid stays put — verified live: clicked at 418, still at 418, order unchanged), and the copy acknowledgement no longer hides behind `:hover`, so where a click legitimately moves a swatch — the palette RECENTRES on the colour you pick, measured index 1 → 2 — the tick follows the swatch instead of blinking on whatever the stale hover points at. Regression test asserts the saved order across a click; it fails on the old wiring with `['#CC3333','#AA1111','#BB2222'] ≠ ['#AA1111','#BB2222','#CC3333']` |
| ColorConverter: `useCopyFeedback`, and the two defects the five copies shared | ✅ | The same eight lines were written out in `CopySwatch`, `ColorFormatItem`, `ColorSummary`, `ExportPanel` and `GradientGenerator`. (1) **The timer outlived its component** — palette swatches are keyed by colour and the palette is derived FROM the colour, so clicking one unmounts most of the grid with a `setTimeout` still pending against it. (2) **Nothing reset the flag when the value changed under it** — the scale keys its swatches by SHADE, so `500` is one instance across every colour picked: copy `#0d5a6b`, change the colour, and for the rest of the two seconds the badge claimed a hex that was never copied. One hook, keyed on the value the acknowledgement is ABOUT, reset in the render phase so no frame paints the stale badge. Second regression test; fails on the old code |
| ColorConverter: the pinned card could put its own footer out of reach | ✅ | A sticky element taller than the space below its `top` offset is pinned with its bottom past the fold and nothing scrolls to it. Measured at 1280×720: **640px available, 641px closed, 881px with "Boshqa rang fazolari" open** — the copy-link footer 241px unreachable. Capped at `calc(100dvh-6rem)` with `overflow-y:auto`, so the scroller exists only when the disclosure is open; verified pinned at `top: 80`, inner scroll 257.5, footer at y **659–683**, inside the viewport. Honest cost: at exactly 720px the closed card is 17px over the cap, so a 720px-tall window sees a hairline scroller it did not before |
| ColorConverter: three more from the same pass | ✅ | (1) **The FAQ was an invalid `<dl>`** — `<dl>`'s content model admits `<dt>`/`<dd>` groups and `<div>` wrappers and nothing else, so a `<details>` child with the `<dt>` buried inside `<summary>` is a definition list with no terms in it. Now `<h3>` inside `<summary>` (valid: summary takes heading content) — and the questions become real document structure. (2) **`ContrastPanel` kept a second copy of the WCAG table** (`>= 4.5`, `>= 3`, `>= 7` spelled out by hand) and called `hexContrast` **five times** to compute one number; it uses `gradeContrast` and one measurement now. (3) **`getColorName` re-ran a 139-entry OKLab scan per call per render** — 12 preset swatches plus up to 20 palette swatches — now memoised on a frozen registry. Also: `toggleFavorite` re-read `localStorage` to answer a question the rendered `Set` already knew |
| ColorConverter: SSR/SEO answered with numbers | ✅ | The route is **`● (SSG)` — prerendered to static HTML at build time**, both locales, so there is no server wait and no loading state to design; the `'use client'` island hydrates on top of complete markup. Verified in `.next/server/app/{uz,en}/tools/color-converter.html`: the H1, the converted values (`oklch(0.433 0.073 217)`), the colour name, the whole 50–950 ramp, the contrast verdict sentence, the seven-format reference prose and all four FAQ **answers** are in the served HTML; `/en` is genuinely English (0 Uzbek hits). JS: **371 KB gz total, of which 106 KB gz (5 chunks) is route-specific** — the rest is the shared shell — against qr-generator's 676 KB. lucide is tree-shaken (21 SVG paths across the route chunks for 12 icons). Remaining console noise is React's dev-only "script tag while rendering" warning about JSON-LD, shared by every route on the site |
| ⚠️ Owner's question: is the suite actually on ONE shared FAQ/disclosure? | ⚠️ | **No, and the third implementation is mine.** Measured across all 17 prerendered tool pages, comparing the `FAQPage` schema against the visible DOM (every `<script>` stripped, HTML entities decoded — otherwise the RSC flight payload carries the JSON-LD text and the grep lies): **15 of 17 routes publish a FAQ schema and render NOTHING — 0 questions, 0 answers.** Of the two that render anything: `latin-cyrillic` (shared Radix `FaqAccordion`) ships **6/6 questions and 0/6 answers**, `color-converter` (native `<details>`) ships 4/4 and 4/4. Three disclosure implementations exist — `FaqAccordion` (LatinCyrillic), Radix `Accordion` used directly (`QrGenerator/StylePanel`), native `<details>` (ColorConverter). **This corrects the row above claiming latin-cyrillic's FAQ "is now visible and reads the same i18n keys the schema does"** — its QUESTIONS are visible; its answers have never been in the document |
| ⚠️ Shared-component adoption, counted | ⚠️ | `ToolHeader` **21/21** ✅ · `CopyButton` 9 · `StatsDisplay` 9 · `DualTextPanel` 8 · `SegmentedControl` 5 · `FaqAccordion` 2 · **`ToolCard` 1** · `ProgressBar` 1 · `BaseModal` 1 · `InfoCard` **0 consumers**. `ToolCard` was extracted "at the sixth consumer" and then never rolled out: **63 hand-copied `rounded-xl border bg-card` blocks** remain across `src/modules/tools/`. Root cause is the working method, not a series of accidents — the suite has been done tool-by-tool ("tool 3 of N", "5th tool"), each pass extracting what that tool needed and moving on, with no rollout step. Rollout needs the owner's scope decision, so it is a **question**, not a silent sweep |
| One shared `Faq`, rolled out to all 17 tool routes | ✅ | Owner picked the full scope. New `src/components/shared/Faq` — a Server Component on native `<details>` + `.disclosure`, **zero client JavaScript** — plus `faqPageSchema()` in `lib/seo.ts` so the visible section and the structured data are built from the SAME array and cannot drift. Each route now derives both from one `getFaqItems(locale)` (the 15 legacy routes already carried `uz`/`en` copies, so no new translation was needed); the two tool-owned components (`ColorFaq`, `ConverterFaq`) keep the CONTENT and delegate the chrome, which is the composition rule the rest of the suite follows. **Measured before → after on the prerendered HTML of all 17 tools: questions on page 2/17 routes → 17/17, ANSWERS on page 1/17 → 17/17.** `/en` verified English on all of them. Not in `@webiston/ui` on purpose: that package is standalone and ships into the Chrome extension, while this needs `next-intl/server` and an app stylesheet rule — same reasoning as `ToolCard` |
| `.disclosure`: a `<details>` that slides | ✅ | Owner: the FAQ "opens instantly" now — correct, and it was a regression against every other disclosure on the site. `::details-content` + `calc-size(auto, size)` at the accordion's own 240ms/200ms and easing, `prefers-reduced-motion` guarded, behind `@supports` so an unsupporting browser gets exactly the previous behaviour. `calc-size` rather than a global `interpolate-size: allow-keywords` — scoped to the class instead of changing how every `auto` on the site animates. Verified with a transition-off probe (the Browser pane is a hidden tab, where Chrome freezes transitions): closed `block-size: 0px` → open `120px`, the content's exact height. Applied to the FAQ and to ColorConverter's two disclosures |
| `ToolCard` rolled out — 4 conversions, 7 deliberately left | ✅ | Owner scoped it to the five refactored tools. Converted the four that are genuinely ToolCard-shaped — a card with a bordered header rule, kicker dot, title and optional right-hand action: `QrGenerator` input panel, `QrPreview`, `PasswordDisplay`, `StrengthMeter`. `QrGenerator` is now 0 hand-copies. **The other seven were left and that is the finding, not an omission:** `AlphabetTable` ×2, `JsonFormatter/InfoSection` ×2, `PasswordGenerator/InfoSection`, `SettingsPanel`, `FormatReference` are headerless containers or use the heading-inside-the-body pattern — wrapping them in `ToolCard` would ADD chrome that was never there, which is not a refactor. Adoption `ToolCard` **1 → 3 tools** |
| ⚠️ `FaqAccordion` now has 0 real consumers | ⚠️ | Both FAQs moved off it, so `packages/ui/src/composites/FaqAccordion` joins `InfoCard` as a shared component nothing imports (the three remaining matches are all prose in docblocks). Deletion needs the owner's explicit yes, so it stays for now — listed here rather than folded into this batch |
| Tool 6 of N: **Base64 converter** | ✅ | Next by the owner-approved order in `constants/ui.ts` (positions 1–5 done), not by preference. Rebuilt onto the suite: module structure (`utils/`, `types/`, `constants/`, `stores/`, `seo/`), module-scope draft store (locale remount — the 5th tool to get it), `SegmentedControl` + `Button` in place of `GradientTabs`/`ShimmerButton`, the toolbar-not-a-card treatment latin-cyrillic established, the shared `DualTextPanel` and the shared `Faq`. Palette classes **8 → 0**, `dark:` **1 → 0**. Route JS **358 KB gz**; both locales prerendered with the H1, the reference prose, the worked `Ali → QWxp` table and all four FAQ answers in the served HTML (verified with scripts stripped) |
| Base64: six defects the maths hid | ✅ | (1) **base64url was rejected** — the validator was `/^[A-Za-z0-9+/]*={0,2}$/`, so every `-` or `_` reported "invalid Base64 format"; that is the alphabet JWTs use, and this site ships a JWT decoder that emits it. (2) **`throw` inside `reader.onerror`** runs asynchronously, so the surrounding `try/catch` never saw it — the throw escaped unhandled and `setIsProcessing(false)` never ran, leaving the tool stuck on "processing" until a reload. `FileReader` is gone; `File.text()`/`.arrayBuffer()` with `finally`. (3) **Images were encoded twice** — `readAsDataURL` yields base64 already, the handler stripped the `data:` prefix and put THAT in the input box, which the encode step encoded again. Modelled explicitly now: the source is text or a file, and a file's bytes are encoded once. (4) **Invalid UTF-8 came back as mojibake** presented as success; a fatal `TextDecoder` says "these bytes are not text". (5) **`String.fromCharCode(...bytes)`** would blow the stack on the 10 MB upload the tool advertises — chunked at 32 KB. (6) **The size readout divided `String.length` by 1024 and called it KB** — the same defect already fixed in the JSON formatter; it is real bytes now, so `Ўзбекистон` reads 20 and not 10. **11 unit + 8 integration tests**, suite 641 → **660** |
| Base64: honesty and i18n | ✅ | Three `alert()` calls removed (the JSON formatter finding again) — errors are values rendered inline with `role="alert"`, and each decode failure names its own reason (alphabet / truncated / not-text) instead of one generic sentence. **Five hardcoded Uzbek strings and the Uzbek download filename** lived inside the hook next to a translator that was already in scope, so `/en` answered in Uzbek. `LocaleMessages` now gets `locale` on this route (14 routes still missing it). Metadata and schemas moved to a `seo/` segment mirroring ColorConverter: the old `featureList` sold "Katta fayllar bilan ishlash" beside a hard 10 MB ceiling plus "Professional interfeys" and `softwareVersion: "2.0"`; keywords 50 → 13 with the Russian block dropped (the site serves `uz`/`en` only) |
| ⚠️ Base64: two files left for approval | ⚠️ | `components/ControlPanel.tsx` (120 lines) and `components/InfoSection.tsx` (82) are now unreferenced — replaced by `ControlBar.tsx` and the server-rendered `Base64Reference.tsx`. Not deleted, and not folded into this batch. Their `GradientTabs`/`ShimmerButton` imports are NOT dead elsewhere: 5 tools still use each |
| ⚠️ Base64 self-review: I shipped the defect I had just removed | ✅ | The direction `SegmentedControl` was wired `onChange={(next) => { if (next !== mode) switchMode() }}` — it **ignored the value it was handed and called a toggle**, which is verbatim the `GradientTabs` wiring the same commit deleted for being "correct only while there are exactly two options". It also conflated two jobs: choosing a direction silently rewrote the visitor's input. Split — the segment sets direction, the arrow between the panels swaps — which is what latin-cyrillic already does. Two tests now pin the distinction; the old one had encoded the conflated behaviour as correct |
| Base64: three more from the same pass | ✅ | (1) **`byteLength(result.output)` ran in the RENDER** — a full `TextEncoder` pass over an output that can be 13 MB, on every unrelated re-render. Moved into the memo, and encoded output is pure ASCII so its byte count is just its length. (2) The dropdown came from `@/components/ui/dropdown-menu`, the back-compat shim, while `@webiston/ui/primitives/dropdown-menu` is the canonical path new code is supposed to use. (3) A hardcoded `id="file-upload"` replaced with `useId()` |
| Base64: three additions, each naming its consumer | ✅ | (1) **Encoding an image now yields a `data:` URI**, not the bare payload — nobody encodes a PNG to admire the base64, and the prefix is the half of the string a person cannot guess. The FAQ already promised this ("the value you need to inline them as a data URI") and the tool did not deliver it. (2) **The decoder strips a `data:…;base64,` prefix**, so pasting one out of a stylesheet or devtools works and the image round trip closes. (3) `DropZone` extracted to `src/components/shared/` **at its second consumer** (strings now props, not a `LatinCyrillicPage.file` lookup) plus the suite's ⌘/Ctrl+Enter and Esc handlers, with the same DOM-containment guard that stopped Escape in a portalled dropdown from wiping the payload |
| Base64: what was deliberately NOT added | ✅ | **Conversion history** — a base64 payload is as likely to be a token as a greeting; ColorConverter persists history because a palette is a project that spans visits, a one-off conversion is not. **Auto-detect direction** — it would guess, and making the guess explicit is exactly what latin-cyrillic had to do. **Save decoded binary as a file** — the tool deliberately refuses binary decode; a download path would reintroduce the whole binary story for a rare case. **MIME 76-column wrapping** — real but niche, a control most visitors would never touch. **A control-character warning on decode** — `Ali` is valid base64 (it decodes to `0x02 0x58`), so typing prose into decode mode yields invisible junk rather than an error; flagging it needs a heuristic that could reject legitimate output, and every mainstream decoder behaves this way. Recorded, not built |

| Tool 7 of N: **JWT decoder** | ✅ | Next by the owner-approved order. Rebuilt onto the suite: `utils/` `types/` `constants/` `stores/` `seo/`, module-scope draft store, `ToolCard`, the shared `DropZone` and the shared `Faq`, `locale` passed to `LocaleMessages`. Two weights instead of eight stacked components: token in on the left, the VERDICT pinned on the right, the three segments underneath — the old layout gave "is this expired" the same weight as "what is the `typ` claim". Palette classes **5 → 0**. Route JS **357 KB gz**; both locales prerendered with the H1, the RFC 7519 claims table and 4/4 FAQ answers in the served HTML |
| JWT: the decode was wrong in four ways | ✅ | It read both segments with `JSON.parse(atob(part.replace(/-/g,"+")…))`. (1) **`atob` returns Latin-1**, so a token carrying `Alisher Oʻtkirov` or any Cyrillic claim came back as mojibake — and usually as a `JSON.parse` throw the catch reported as "invalid token format" for a perfectly valid token. (2) **No padding handling**, so whether a segment decoded depended on its length modulo four. (3) **One catch for three different failures** — "not three parts", "not base64" and "not JSON" all printed the same sentence; they are now separate reasons and name WHICH segment. (4) **`JSON.parse("123")` is a number**, and the old code reported `isValid: true` then read `.alg` off it. Fixed by reuse, not by a third implementation: `decodeBase64` moved to `src/lib/utils` **at its second consumer** |
| JWT: the expiry answers were unsound | ✅ | The headline question this tool exists for. (1) **A token with no `exp` reported "not expired"**, which reads as "checked, and fine"; it now says "no expiry claim" and the status is `null`, not `false`. (2) **`exp: 0` was treated as absent** because zero is falsy. (3) **A string `exp` was compared with `now > exp`** — a lexicographic comparison that silently answers nonsense; claims are now type-checked as RFC 7519 NumericDate. (4) The verdict is a sentence with relative time (`in 23 hours` / `2 days ago`) instead of a raw date in one of four equal cards. `now` is state set after mount, because rendering "expires in 3 hours" from `new Date()` during render is a hydration mismatch by construction |
| JWT: honesty on a security tool | ✅ | The schema advertised **"JWT validatsiya"** and **"Xavfsizlik tekshiruvi"** on a page that does not verify signatures and deliberately cannot — verifying needs a secret the tool should never ask for. Removed, and the FAQ now says why in full. New: **`alg: none` is called out** where it appears (the oldest JWT attack; the old tool rendered it as just another algorithm name), the signature stays hidden behind a toggle and is never decoded, and the empty state states that the token never leaves the browser. Five `alert()` calls → inline `role="alert"`. Three sample tokens were **fixed strings whose `exp` had all passed**, so by 2026 every sample — including the one labelled "Standard JWT" — read expired; they are built against the current clock now. `inputStats` counted words and lines of a JWT, which are always 1 and 1 — replaced by the segment count. **9 unit + 9 integration tests**, suite 662 → **680** |
| ⚠️ JWT: eight files left for approval | ⚠️ | `ControlPanel` (167), `InfoSection` (131), `TokenInfoCards` (91), `InputPanel` (67), `SignatureSection` (47), `StatusBadge` (26), `SignatureInfo` (25), `ErrorDisplay` (23) — 577 lines, all now unreferenced. Not deleted, not folded into this batch. Together with the two Base64 leftovers that is **10 files awaiting one yes** |

| ⚠️ JWT self-review: I wrote a SIXTH copy of `formatFileSize` | ✅ | Found while reviewing tool 7, in tool 6. `Base64Converter.tsx` carried its own `formatBytes` — in a repo whose own record reads "5 copies → `src/lib/utils/format.ts`". Root cause named: **`format.ts` was never re-exported from the `lib/utils` barrel**, so `@/lib/utils` could not reach it and three tools import it by deep path instead. Barrel fixed, duplicate deleted, and the tool's `units` i18n keys went with it (KB/MB are not translated anywhere else either) |
| JWT self-review: four fixes on my own diff | ✅ | (1) **`now` was set once on MOUNT**, so a token pasted ten minutes into a session was judged against a ten-minute-old clock — a token that had just expired still read "expires in 5 minutes". It re-reads per token now. (2) **`relativeTime` was called without an explicit `now`**, which makes it call `Date.now()` internally — the exact hydration hazard the `now` state exists to remove; the invariant is explicit rather than accidental. (3) **The lifetime row abused `relativeTime` to format a DURATION** (`relativeTime(new Date(seconds*1000), new Date(0))`), which renders "in 24 hours" for a length of time. Row removed and `lifetimeSeconds` removed with it rather than left as dead API — `formatDuration` in `lib/utils` was checked first and is a `mm:ss` clock, wrong for this. (4) **`JSON.stringify` ran twice per part per render**, once for the copy button and once for the block, over a payload that can carry a large permission array |
| JWT self-review: one addition, `iat` and `nbf` as dates | ✅ | Both claims were parsed, typed and then thrown away. A timestamp printed as `1516239022` is the one thing on that page a person definitely cannot read, and the summary already had `format.dateTime` in hand — so it costs no new machinery. Deliberately NOT added: signature verification (needs a secret this page must never ask for), a live countdown tick, a claims-as-table view instead of raw JSON (developers want the JSON), and history of decoded tokens (they are credentials) |

| Tool 8 of N: **URL encoder** — one encoder for two different jobs | ✅ | The central defect, and it made the tool WRONG rather than dated: one button running `encodeURIComponent` over whatever was in the box. That function escapes `:` `/` `?` `&` `=`, which is right for a query VALUE and destroys a whole URL — and the tool's own headline sample was `https://webiston.uz/search?q=hello world&lang=uz`, so the demo produced `https%3A%2F%2Fwebiston.uz%2Fsearch%3Fq%3D…`. Somebody who came to fix the space in their link got something unusable and the page never said which of the two jobs it was doing. There is now a **scope** control: `value` (`encodeURIComponent`, form-encoded) and `whole` (`encodeURI`) — two standards, a visible choice, no guessing |
| URL encoder: the same mistake on the way back | ✅ | Form submissions encode a space as `+`, and `decodeURIComponent("a+b")` returns `"a+b"` — so **every query string copied out of a browser address bar decoded wrong, silently**. Value mode converts `+` to `%20` before decoding (and encoding a literal plus now yields `%2B`); whole-URL mode leaves it alone, because in a path a plus is an ordinary character. Also fixed: **the third occurrence of `throw` inside `reader.onerror`** (async, so the surrounding `try/catch` never sees it), three `alert()` calls, a **private `analyzeUrl` sitting beside the one `lib/utils/url.ts` already exports**, `inputStats`/`outputStats` counting words and lines of a URL (always 1 and 1), and **two file limits that disagreed by 10×** — a 10 MB upload accepted, then refused by a 1 MB text ceiling after it had been read |
| URL encoder: the breakdown the old tool stopped one step short of | ✅ | It parsed the URL, printed protocol / host / path / search as four rows, and left the query as one opaque `?q=hello%20world&lang=uz` blob — the exact thing a person opens a URL tool to read. Each parameter is its own row now with its value decoded, and repeated keys survive: `readQuery` uses `URLSearchParams` rather than `extractQueryParams`, which returns a plain object and so collapses `?tag=a&tag=b`. The panel renders only when the text really parses as a URL, so it never guesses. Palette classes **7 → 0**, `dark:` **1 → 0**, route JS **360 KB gz**, both locales prerendered with the reserved-character table and 4/4 FAQ answers in the served HTML. **10 unit + 9 integration tests**, suite 680 → **699** |
| ⚠️ URL encoder: two files left for approval | ⚠️ | `components/ControlPanel.tsx` (118) and `components/InfoSection.tsx` (114), now unreferenced. That makes **12 files across three tools awaiting one yes** |

| ⚠️ URL encoder: I fixed the maths and got the PRODUCT wrong | ✅ | Owner sent two screenshots — old production beside the rebuild — and the rebuild was the confusing one. The value/whole-URL distinction is real, but I made it a decision the visitor had to take BEFORE the tool would answer: two segmented controls, four combinations, and one of them (decoding text that was never encoded) returns the input verbatim. Their screenshot showed both panels holding the same 108 characters, so the tool looked like it did nothing. That is the engineer's fix, not the product's. **The tool decides and says what it decided**: direction defaults to **Avto** with the resolution printed beside it (`Avto: ochilmoqda`) — latin-cyrillic's own pattern — and the scope control appears **only while encoding**, because decoding has one sensible answer and offering a second as an equal choice was half the confusion. Pasting a link now just works: measured, the default output for `https://…/search?q=hello world` went from `https%3A%2F%2F…` to `https://…?q=hello%20world` |
| URL encoder: three things the page now says out loud | ✅ | (1) **The no-op is named.** "There was nothing to decode here" instead of two identical panels — the exact state from the bug report — with separate wording for the encode direction, because `encodeURIComponent("hello")` also returns it verbatim. (2) **Double encoding is detected** — the `%2520` case, a URL that went through an encoder twice via a redirect parameter or a proxy. It is the single hardest thing to work out unaided, because the output still looks like gibberish and nothing explains why; there is now a one-click "decode once more". (3) **Per-parameter copy** — one value is usually the reason someone opened the tool, and getting it should not mean selecting it out of a wall of text |
| URL encoder: three defects found in my own rework | ✅ | (1) The no-op notice fired while ENCODING too and said "nothing to decode" — wrong sentence, now direction-specific. (2) **The breakdown vanished the moment the scope was overridden to `value`**, because `https%3A%2F%2F…` is not a parseable URL — even though the INPUT still was one; both sides are candidates now, encoded side first. (3) The **`ToolHeader` copy led with the standards distinction** — the very thing that confused the owner — instead of the job; it now opens with "turn the %20 and %3A in a link back into something readable", and the route metadata says the same thing so the search result matches the page. Also fixed from the screenshot: the parameter count sat 8px above the first box and read as part of it |
| Tool 9 of N: **hash generator** — the shipped MD5 was not MD5 | ✅ | The headline, and it is not a style problem: `simpleMD5` was `hash = (hash << 5) - hash + char` — a 32-bit string hash — printed four times to reach 32 characters. **Measured: `Hello, World!` answered `5955b8155955b8155955b8155955b815` where RFC 1321 and `md5sum` both say `65a8e27d8879283831b664bd8b7f0ad4`.** Every published MD5 anyone checked against it disagreed, and the only thing the tool told them was that their file was corrupt. Replaced with a real RFC 1321 implementation over `Uint8Array` (`utils/md5.ts`, ~90 lines, K derived from `sin` rather than pasted as 64 magic numbers), verified 7/7 against the RFC's own appendix A.5 suite and 201/201 against node's `crypto` for every input length 0–200. 1 MB in 14 ms |
| Hash generator: "file hash" hashed the file's TEXT | ✅ | `reader.readAsText(file)` then hash the string — so a BOM, CRLF endings or any non-UTF-8 byte gave a digest `sha256sum` disagrees with, which defeats the only reason to hash a file. Now `file.arrayBuffer()` and hash the BYTES. **Proved in a test: four bytes `ff fe 00 41` hash to `6e153708…`; the old path replaced each bad byte with U+FFFD and answered `b4668f54…`.** Side effect — the file-type allowlist is gone, because nothing pretends the contents are text: any file works |
| Hash generator: two more silent-wrong-answer defects | ✅ | (1) **`setTimeout(…, 300)` with no `clearTimeout` was called a debounce.** Every keystroke queued a full async run and they could finish out of order, so the panel could settle on the digest of text already changed. Now one effect whose cleanup cancels the stale run — no timers, no sequence numbers. (2) **Toggling an algorithm hashed with the PREVIOUS selection**: the regeneration was scheduled from inside the `setSelectedAlgorithms` updater, closing over the pre-toggle array, so turning SHA-512 on showed results without it until the next keystroke — and it ran a side effect inside a state updater, which React double-invokes in dev. The checkboxes are gone entirely: five digests of typed text cost microseconds, so the choice bought nothing and cost a bug class |
| Hash generator: the two features a checksum tool needs | ✅ | (1) **Verify.** Paste a published checksum and be told whether it matches AND which algorithm it is — identified by length, which is unique across MD5/SHA-1/SHA-256/SHA-384/SHA-512 in both hex and Base64. All four published shapes are read: bare, `sha256sum` output, BSD `shasum` output, `sha256:…`. The route's old structured data listed "Hash taqqoslash" in its `featureList` for a tool that could not compare anything. (2) **HMAC-SHA**, the keyed digest Stripe/GitHub/Telegram sign webhooks with — the feature paid API clients charge for, ~15 lines on Web Crypto, verified against RFC 4231. There is no HMAC-MD5 and the page says why rather than dropping a row silently. Plus SHA-384 (free, and what SRI wants) and a hex/Base64 toggle |
| Hash generator: the page was teaching the mistake | ✅ | `InfoSection`'s use-case list opened with **"Parol hashing"** and the schema's `featureList` repeated it as `"Parol hash yaratish"`. Hashing a password with SHA-256 is the thing you must not do — these functions are built to be fast, which is what an offline cracker wants. The reference section now says so with bcrypt/scrypt/Argon2 named, and it is one of the five FAQ answers. Also corrected: the `Low → Very High` ladder rated **SHA-1 "Medium"** eight years after SHAttered; status is now two values, `broken` or `standard`, and the badge appears only on the broken ones |
| Hash generator: structure, tokens and SEO | ✅ | Module went 8 files → 20 with `constants/ types/ stores/ utils/ seo/`; `export { default }` from a non-page file replaced with named exports; route deep-imports instead of pulling the 21-module `@/modules/tools` barrel, and passes `locale` to `LocaleMessages`. Gone: `GradientTabs`, two `ShimmerButton`s, the fake macOS traffic lights, 27 `t("x") \|\| "fallback"` fallbacks that hid missing keys, and ALGORITHM metadata hardcoded in **Uzbek inside the hook** so `/en` read Uzbek. SEO: ~90 keywords across three languages → 10 (the **20-term Russian block** was for a locale the site does not serve; `"bulk hash generation"` and `"batch hash processing"` were never features). Palette classes **15 → 0** and `dark:` **6 → 0** in the new files. Both locales prerendered with 5/5 FAQ answers, 5/5 reference rows and the password warning in the served HTML. **20 unit + 13 integration tests**, suite 704 → **750** |
| Hash generator: two defects found in my own rework | ✅ | (1) **I disabled the field the visitor was typing into.** `DualTextPanel` disables its textarea while `isProcessing`, and I wired sub-millisecond text hashing into that flag — so focus was lost on every character and the result panel flashed "Ishlanmoqda...". Caught by a test written to fail first; `isProcessing` now covers file reading only. (2) **`verifyChecksum` fell through from the hex length check to the Base64 one**, so a 44-character hex string would be named a SHA-256 on a coincidence (44 is SHA-256's Base64 length). One rendering or the other, never both |
| ⚠️ Hash generator: four files left for approval | ⚠️ | `components/ControlPanel.tsx` (137), `InfoSection.tsx` (100), `DetailedResults.tsx` (91), `AlgorithmSelector.tsx` (76) — 404 lines, now unreferenced, and they hold the 15 palette classes the token guardrail still counts. Plus `HashGeneratorOptions` in `src/types/tools.ts` (+ its re-export in `src/types/index.ts`), which no code reads. That makes **16 files + 1 type across four tools awaiting one yes** |
| ⚠️ Hash generator: one feature removed, for veto | ⚠️ | The **JSON download** is gone. It emitted a metadata envelope wrapping five strings; the `.txt` now emits `<algorithm>  <digest>` per line — the shape a checksum file uses, so it can be diffed against a published one instead of read by eye. Say the word and it comes back |
| ⚠️ Tool 10 of N: **UUID generator** — the row below this one was not true | ⚠️ | Written by a different model and left on the branch **red**: `pnpm typecheck` failed with 4 errors and `pnpm test` failed on the tool's OWN test, which asserted `validateUuid(NIL) === true` against a regex requiring a version nibble of 1–5 (Nil is all zeroes). It claimed the shared `Faq` and a `seo/` segment: the `seo/` files existed but **nothing imported them** — the route still carried the old inline blob — and the FAQ was four questions hardcoded in the route file in two languages while `faq.*` keys sat translated and unused in both bundles. Recorded here because the claim outlived the code, which is the failure mode this file exists to prevent |
| Tool 10 of N: **UUID generator**, rebuilt for real | ✅ | Onto the suite: `constants/ types/ utils/ stores/ seo/`, module-scope draft store keyed by `version:count` (so a locale remount keeps the batch but changing the version regenerates it), `ToolCard`, `SegmentedControl` ×2, the shared `Faq`, deep route import instead of the 21-module `@/modules/tools` barrel, `locale` passed to `LocaleMessages`. Route metadata was **~100 keywords across three languages** including a Russian block for a locale we do not serve, plus `softwareVersion: "2.0"` and a `featureList` selling "UUID validation", "Statistika va analytics", "Alohida UUID nusxa olish" and "Professional interfeys" — the page had no validator, no per-value copy and no statistics. Verified on the prerendered HTML of both locales: **5/5 FAQ questions and 5/5 answers on the page**, `/en` genuinely English, 0 Russian keywords, 0 `softwareVersion` |
| UUID: v1 was a float where a 60-bit integer was required | ✅ | `(Date.now() + 12219292800000) * 10000` is ≈**1.7e16**, past `Number.MAX_SAFE_INTEGER` (9.007e15), so the low bits of every v1 timestamp were quantised to whatever the double could represent — the value still looked like a UUID, it just did not carry the time it claimed. Rewritten on `BigInt` (as `BigInt()` calls, because the app's tsconfig targets ES2017 and a `123n` literal is the one syntax TypeScript cannot downlevel), with a fixed clock sequence per page load, a sub-millisecond counter, and a random node carrying the **multicast bit** RFC 9562 §5.1 requires of a node that is not a MAC address. Proved against the RFC's own worked example: `c232ab00-9414-11ec-b3c8-9f6bdeced846` decodes to **2022-02-22T19:22:22.000Z** |
| UUID: **v7**, the version people now come for | ✅ | Owner approved. RFC 9562 (May 2024, which obsoletes 4122) — 48-bit Unix ms, then 74 random bits, so a column of them sorts by creation time and inserts land at the END of a B-tree index instead of scattering across it. Monotonic **inside** one millisecond via the RFC's 12-bit counter in `rand_a` (§6.2 method 2), because without it "generate 1000" returns an unsorted list and quietly disproves the one property the version is chosen for. Test asserts 500 values drawn in a burst are already in sorted order; the §A.6 example decodes to the same instant as the v1 one |
| UUID: an inspector, which is half the job the tool never did | ✅ | A generator answers "give me one"; the question a developer arrives with just as often is "what is this value in my log line". Paste any UUID — hyphenated, compact, `{braced}`, `urn:uuid:`, any case — and get the version, the variant (RFC / NCS / Microsoft / reserved) and, for v1/v6/v7, the embedded creation time. It reads **every** version the RFC defines, not the four this tool makes, because a v5 from somebody else's system is exactly the value you cannot identify by eye. The timestamp is printed as `2022-02-22 19:22:22 UTC` rather than through `useFormatter`: the instant IS UTC, and Chrome has no Uzbek date patterns so the locale path renders `2026 M08 6` |
| UUID: what the panel does with the answer | ✅ | The batch was one `<pre>` of text, so taking ONE identifier — the overwhelming majority of visits — meant selecting 36 characters without catching the line above. Now: a single value gets the panel to itself at reading size, a batch is a numbered list with **per-row copy**, and the list **scrolls in its own 26rem box** (owner's report: 100 values made a 4,000px column that pushed the inspector, the reference and the FAQ off the end of the document and scrolled the controls away with them). Rows are still chunked at 100 with "Yana 100 ta ko'rsatish" inside the scroller — 1000 rows each with a copy button is 1000 mounted components |
| UUID: five more defects in what was there | ✅ | (1) **`copyAll` claimed success before the clipboard answered** — a `.then()` with no `.catch()`, so a refused write both acked and raised an unhandled rejection; the shared `CopyButton` owns that interaction now. (2) The bottom copy control was **labelled with the panel's own title** (`{copied ? t("copied") : t("title")}` → a button reading "Natijalar"). (3) `isGenerating` was set true and false in the same synchronous block, so the spinner it drove could never paint. (4) The store held **pre-formatted** strings, so switching format left the old shape on screen until the next generate. (5) Case was a fourth entry in the format list, which made `compact` + uppercase unreachable — they are two axes and are now two controls |
| UUID: 722 lines of dead code, and the guard that let it rot | ✅ | Owner approved. `ConfigPanel` (264), `InputPanel` (122), `InfoPanel` (138), `ResultsPanel` (131), `StatsPanel` (62) + their barrel — unreferenced, and the **source of all four typecheck errors** on this branch: they imported types their own hook no longer exported. Token guardrail: **35 hits removed** from the baseline. **17 unit + 12 integration tests**, suite 750 → **779** |
| Tool 11 of N: **OG meta generator** — it did not escape its own output | ✅ | The headline, and it made the tool WRONG rather than dated: every tag was built as `content="${value}"` with **no escaping at all**. A title with a straight quote — `React "Hooks" darslari` — produced `content="React "Hooks" darslari"`, which ends the attribute after `React ` and turns the rest of the headline into stray attributes on the element; an ampersand was invalid markup for the same reason and a `<` put a tag inside a tag. This is a tool whose only output is HTML somebody pastes into their `<head>`. Now one `escapeAttribute` with the `&`-first ordering (replacing `"` before `&` yields `&amp;quot;`, which renders the entity as visible text), pinned by tests |
| OG: it invented metadata about pages it knew nothing about | ✅ | Choosing `book` emitted `book:isbn content="978-0000000000"`; `profile` emitted `profile:first_name content="First"`; `music.song` emitted `music:duration content="240"` and `music:album content="Album Name"`; `video.other` emitted a 1280×720 `video:width`/`video:height`; and `article` stamped `article:published_time` with the moment you last pressed a key. None of it was ever asked for, and all of it would have gone into a real page as fact. The type list is five now — only types whose tags this tool can honestly produce — and a field left empty produces no tag. Also gone: `og:image:alt` filled with the TITLE (or the literal `"Image"`), and the always-appended `robots`/`googlebot` lines, which are the default anyway and were an opinion inserted into someone else's page |
| OG: the output trailed one keystroke behind | ✅ | Output lived in `useState` and was written by a `setTimeout(…, 0)` fired from every field update — which closed over the PREVIOUS `metaData`, so the generated tags were always one character old. A `useEffect` on the same state then regenerated them a second time, which is what hid the lag well enough to ship. Output is derived now, so there is no second copy of the truth to fall behind. The hook went **601 → 108 lines**; `t?: any` (the translator passed INTO the hook, the pattern already removed from the password generator) went with it, and with it five "templates" that filled the visitor's form with **this site's URL, this site's logo and a paragraph of marketing copy about Webiston** |
| OG: the form refused the keystrokes it disliked | ✅ | `updateField` returned early above 70 characters for the title and 200 for the description — no state update, and an `onError` callback that the component never passed, so the field simply **stopped accepting input with no explanation**. A long title is legal; it is just truncated. Everything is typeable now and a counter says where the cut falls, in `warning` rather than `destructive`. The default form also no longer arrives pre-filled with `webiston.uz/logo.png` and `webiston.uz/tools`, which anyone who did not notice published as their own share card |
| OG: the preview earns its place, and the checks say what to do | ✅ | The old preview stacked a Facebook, a Twitter and a Telegram block one under the other, under a row of five platform buttons **with no `onClick`** — the control that looked like the way to choose was decorative. One card now, switched by the shared `SegmentedControl`, each platform applying **its own** truncation, so the same title visibly fits on Telegram and gets cut on X. And the "SEO score out of 100" (a 30–60 character title was worth 25 points, a site name 10) is replaced by named issues: a **relative `og:image` URL** — the single commonest reason a share card has no picture — the real image dimensions and ratio measured by loading it in the browser, an unreachable image, a missing alt, and a `summary_large_image` with no image to be large with |
| OG: the two shapes people paste into | ✅ | HTML for a `<head>`, and an App Router **`metadata` export** — this site runs on Next and so does much of who opens this page, and hand-translating twelve `<meta>` tags into the `openGraph` object shape is exactly the mechanical step worth removing. The two renderers are separate functions on purpose: the Next one escapes for a JavaScript string via `JSON.stringify`, and HTML-escaping there would paste `&quot;` into the file. What it replaces offered "raw" and "formatted", where formatted meant the same tags wrapped in a whole boilerplate HTML document. Route rewritten onto `seo/` + the shared `Faq` + a server-rendered reference; palette classes **49 → 0**. **23 tests**, suite 779 → **802**; both locales prerendered with 5/5 FAQ answers |
| OG self-review: four defects in my own diff | ✅ | Reviewed as a diff rather than as code I had just written. (1) **`escapeAttribute` escaped the apostrophe** to `&#39;` — correct, unnecessary inside a double-quoted attribute, and this audience writes Uzbek, where `o'` and `g'` are LETTERS: every title would have shipped as `O&#39;zbekiston` in the block the visitor pastes. (2) **The preview used `twitter:card` for all four platforms** — an X tag. Setting it to `summary` shrank the Telegram card, and Telegram has never read that tag; everywhere else the image's own width decides, so the preview now answers the same question the platform will. (3) A `twitter:title` emitted **only when there was no description** — a rule with no reason behind it. (4) An SVG with no intrinsic size reports **0 × 0**, which the checks called "smaller than 200 pixels" rather than "cannot be measured". Also added: Escape-to-clear (every other refactored tool has it) and a sample image on **our own `/api/og`** instead of picsum.photos — the tool's own demo must not be the one request that contradicts its privacy line, which was itself reworded to say the image IS opened by the browser |
| Shared `CodeHighlight`: two of four branches were broken | ✅ | Owner asked why the OG output was a wall of grey when the JSON formatter colours its own. Answer: the shared highlighter was half-converted and half-wrong. **`[^&gt;]` is not "any character except `>`"** — it is the character class `&`, `g`, `t`, `;`, so the tag rule stopped at the first `t` in `<meta` and the comment rule could not match a comment containing `g`. The attribute-value rule broke on any value holding an entity. And chained `String.replace` calls cannot colour markup at all: the second rule sees the `class="…"` the first one just inserted and colours THAT — every branch is now a single pass deciding per match. Colour moved onto `--chart-*` (JSON already was): **14 palette classes + their `dark:` twins → 0**, and the `bg-zinc-100 dark:bg-zinc-900` shell dropped so the block matches the panel around it — the JSON formatter's own plain-`<pre>` fallback above 300 KB looked like a different component before. Added a `typescript` language. **6 tests**; verified in the prerendered OG HTML (15 token spans, server-rendered) |
| OG: the two additions the competitors are bought for | ✅ | Owner asked what the leading tools have that we do not. Answer was **paste-your-`<head>`** and **re-scrape links**, and both are in. (1) **Import**: paste a page's head — or a bare block of tags — and the form fills from it, `og:*` taking precedence over the plain tags exactly as a crawler applies it, `<title>` and `link[rel=canonical]` as fallbacks, `name="og:title"` accepted because Facebook accepts it. `DOMParser`, whose document is INERT — no browsing context, so nothing is fetched and no script runs — which is what makes parsing a stranger's markup safe with no server. metatags.io and opengraph.xyz do this by FETCHING the URL server-side; that is a request-forgery surface to harden forever, and nobody edits tags for a page they cannot open. Values the form's own options cannot express (`og:type=music.song`, `uz-UZ`) are dropped rather than written in. **10 unit + 3 integration tests.** (2) **Re-scrape links** with the visitor's own URL already in them — Facebook Sharing Debugger, LinkedIn Post Inspector, and **@WebpageBot** for Telegram, which has no web debugger and matters most for this audience. The FAQ has said "re-scrape it" since the rebuild and left them to find the tools by name |
| ⚠️ OG: two of my own defects, found by writing a test | ✅ | Both would have shipped. (1) **A dot inside a next-intl key is NESTING**, so `t("types.video.other")` looked for `types → video → other`, found nothing, and rendered the key path — the select's Video option literally read `OgMetaGeneratorPage.form.types.video.other` in the built HTML. (2) **ICU parses `<head>` as a rich-text tag** with no handler, so every message containing one — the import hint, the tag example, and the Next.js FAQ **answer** — fell back to its key path too. My earlier verification missed the FAQ one because I compared the schema text against the page text and BOTH came from `t()`: two identical key paths match perfectly. The build check is now "no `SomethingPage.x.y` appears in any prerendered tool page" (0 across all 34), and an integration test asserts the same on the rendered DOM |
| OG: a slow image said there was no image | ✅ | Owner's report, with a real URL that took a second: the card showed "Rasm yo'q" while the fetch was still in flight, so the tool denied a picture it was in the middle of loading. Now a shimmer in the reserved box plus `aria-busy`, and a spinner line in the checks — **deliberately not a progress bar**: an `Image()` load reports nothing between start and finish, and a bar that invents its own position is a lie about a wait. Sample image moved to this site's own `/api/og` (1200×630, ours, no third party); its first render is slow because Satori rasterises on demand, which is exactly the case the loading state exists for |
| ⚠️ `/api/og` printed a bare `/` on every card without a path | ✅ | Owner spotted it on a rendered image. `/^\/?[\w\-/]{0,120}$/` **matches the empty string**, and `"".replace(/^\/?/, "/")` is `"/"` — truthy, so the caption line fell through to a lone slash instead of the tagline. Every request without a `path` rendered it, which is every one outside the book chapters. Extracted as `normaliseBreadcrumb` (empty rejected first, bound `{1,120}`) with **3 tests** — the first this route has had |
| OG: shareable link, and three more platforms | ✅ | Owner asked whether to make it shareable: yes, and it is. The draft is mirrored into the address bar on every keystroke via `history.replaceState` (a router navigation per keystroke schedules work nobody asked for — the call the URL encoder and `/tools` already made), short keys, empty fields omitted, and a **Havola** button that copies the full URL. Built on CLICK, never during render: `window.location.origin` does not exist on the server, so reading it while rendering is a hydration mismatch by construction. Values a link carries for the three fixed-list fields are validated, not trusted — `?ty=music.song` cannot put the select into a state its own options lack. Previews went **4 → 7**: WhatsApp, Slack and Discord, each with its own truncation and its own row in the reference table. **6 more tests**, suite **830** |
| OG: what was deliberately NOT added | ✅ | **Fetching a URL server-side** — the one thing metatags.io and opengraph.xyz have that we do not. It is a permanent request-forgery surface (private addresses, redirect chains, timeouts, response size), and the `<head>` importer reaches the same place with no server: nobody edits tags for a page they cannot open. **An OG image builder** on our own `/api/og` — we own the endpoint, but the image it returns lives on `webiston.uz`, so a visitor's card would point at OUR domain and break the day we change the route. **A dark-mode card preview** — real, but it changes nothing about the tags |
| Tool 12 of N: **Lorem Ipsum** — the output IS the product | ✅ | Rebuilt onto the suite: `constants/ types/ utils/ stores/ seo/`, module-scope draft store (9th tool), toolbar-not-a-card, shared `Faq`, server-rendered reference, deep route import, `locale` to `LocaleMessages`. The generator is where the work went, because a filler tool is judged entirely on what it emits: sentences were **5–15 words with no punctuation but a full stop**, which reads as a list rather than prose and defeats the one job filler has. Now 4–18 words with a comma inside the long ones, placed between the third word and the third from last — never after the first word, never against the stop. Paragraphs 3–7 sentences. Palette classes **14 → 0** |
| Lorem: the Uzbek word list had its own LABEL as its first word | ✅ | Every Uzbek text began `O'zbek Lorem matn sahifa…` — the name of the option that produced it. Also `malumot` spelled without the tutuq belgisi, `zamonaviy` twice, and 40 words of product jargon (`platforma`, `innovatsiya`, `optimizatsiya`) rather than words of the length real copy has. Rewritten to **113 unique everyday words** carrying the marks Uzbek layout must survive — `o'`, `g'`, `ch`, `sh` — because that is the whole argument for the bank: `o'` and `g'` are single LETTERS written with an apostrophe, so a column sized against Latin filler overflows on the real copy. Pinned by tests that assert the label is absent and `ma'lumot` is present |
| Lorem: four more defects | ✅ | (1) **`loadSample` generated inside a `setTimeout(…, 100)`** that closed over the settings from BEFORE the sample was applied — the comment above it read "generate after a small delay to ensure state is updated". (2) `generateText` wrote `""` and then the result, rendering an empty panel between two frames. (3) **The `bytes` mode sliced `String.length`** and called it bytes; it is `TextEncoder`-exact now, verified at 16/120/512. (4) **`startWithLorem` was offered for all five word lists and silently ignored on four** — `Lorem ipsum dolor sit amet` is Latin, so the toggle now appears only for the classic list. The classic line is also quoted properly: a random comma used to land INSIDE it, which a test caught |
| Lorem: what the page is now | ✅ | One weight instead of five. The old page stacked a config card, a stats strip, a `DualTextPanel` **whose right half was a stat sheet rendered as text** (character/word/line/paragraph counts in a monospace box), an info card and a help card. Now: toolbar, then the text — set in the READING face at 68ch when it is prose, and through the shared `CodeHighlight` when the format is HTML, because those are two different things to look at. Format is derived, so switching plain↔HTML never re-rolls the words. Three measured facts in the card header. Download writes the text and nothing else; what it replaces wrapped it in a hardcoded-Uzbek banner, a timestamp, a settings dump and two `---` rules. **19 unit + 8 integration tests**, suite 830 → **849**. Owner approved deleting `ConfigPanel` (238), `HelpSection` (105) and `InfoSection` (65); token guardrail **188 → 219 hits removed** |
| Lorem self-review: three defects in my own diff | ✅ | (1) **The stats measured the OUTPUT, not the text.** Ask for 512 bytes, switch to HTML, and the panel read 519 — seven characters of `<p></p>` counted as filler. The counts sit beside the controls that requested them, so they answer the same question: measured on the plain text now. (2) **Leaving the bytes unit generated 100 paragraphs.** 512 clamped to the paragraph maximum, so one click on "Abzas" produced ~40,000 characters nobody asked for; each direction now carries its own default. (3) The result panel was titled with the WORD LIST, so it read **"Bacon"** — a region name that says nothing about the region, while the bank is already visible in the toolbar above it |
| Lorem: filler in Uzbek **Cyrillic**, which no competitor has | ✅ | Uzbek is written in both alphabets and a large share of publishing here is still Cyrillic — and the two are not the same width: `sh` and `ch` collapse to single letters (ш, ч) while `x` becomes ҳ, so a column laid out against Latin filler is wrong for a Cyrillic page in the other direction. The list is **derived** from the Latin one through `@webiston/transliteration` — the package this site already ships — so there is no second vocabulary to keep in step, and it is converted once and cached rather than 113 words through the engine per keystroke. Verified end to end: `маълумот`, `саҳифа`, `ўқувчи`. Byte presets added (128/512/1024/5120 — the sizes a field limit comes in). **22 tests**, suite **852** |
| Tool 13 of N: **device info** — detection written for 2015 | ✅ | Every rule was wrong in the same direction. **`ua.includes("Chrome") && !ua.includes("Edg")` called every Chromium browser Chrome** — Opera, Brave, Vivaldi and Arc all ship `Chrome/`. **Chrome and Edge on iOS were reported as Safari**: they are `CriOS/` and `EdgiOS/`, neither of which contains `Chrome` and both of which contain `Safari`. **An iPad was reported as a desktop** — since iPadOS 13 Safari sends a Macintosh user-agent, and the tell is that no Mac has a touch screen (`maxTouchPoints > 1`). And the version was **fabricated precision**: Chrome froze its user-agent version in 2022, so `Chrome/120.0.6099.109` was a number the browser no longer publishes there. Client Hints (`userAgentData.brands`, `getHighEntropyValues`) are read first now, with the string as fallback — and the panel shows WHICH source answered, because that is what says whether the version is real. **11 unit tests against real user-agent strings** |
| Device info: the values that move | ✅ | Four things change without a page load — going offline, rotating, resizing, switching the system between light and dark — and a page that reports them once and then lies until you press refresh is worse than one that does not report them. All four are subscribed. New rows that no version of this page had: **the viewport** beside the screen (CSS media queries measure the window, so that is the number a designer needs), `screen.orientation.type` instead of comparing width to height (which calls a narrow desktop window "portrait"), and a **preferences** group — colour scheme, reduced motion, contrast, forced colours — which is the group a front-end developer actually opens this page for |
| Device info: what the page is FOR | ✅ | New server-rendered privacy note, and it is the point of the tool rather than a footnote: not one value on the page needed permission, and together they are a fingerprint — screen size, time zone, language list, core count, theme preference. That is why clearing cookies does not stop tracking. Rows the browser will not answer are **kept and labelled** rather than hidden, because "Firefox declines to implement this, deliberately" is itself the answer. Layout: six data-driven cards in a grid, one `InfoGroupCard` instead of five near-identical panels — each of which carried three fake macOS window buttons and a blue status dot, fifteen decorative circles on one page. Palette classes **27 → 0**; the JSON export is built FROM the rendered rows, so the file cannot describe a different device than the page. **16 tests**, suite **868**. Owner approved deleting the 7 replaced panels (662 lines); token guardrail **219 → 278 hits removed** |
| Dead code across five tools, deleted | ✅ | Owner approved the full backlog. **21 files, 2,298 lines**, every one verified to have zero importers by resolving each `from "…"` specifier (a name-based grep says `InfoSection` is used everywhere, because eight other tools have their own): OgMeta ×5 · JWT ×8 · Hash ×4 · Base64 ×2 · URL ×2, plus the `HashGeneratorOptions` type and its re-export. Token guardrail: **162 hits removed** from the baseline |
| Hash generator: audited, and it holds | ✅ | Re-read against the suite rules rather than trusted: **0 palette classes, 0 `dark:`, 0 hardcoded Uzbek** in the live files, real `seo/` segment wired to the route, `jsonLd()` escaping, `locale` passed to `LocaleMessages`, module structure complete, MD5 verified against RFC 1321 and node's `crypto`. One gap fixed: `CopyButton`'s accessible name is hardcoded **Uzbek** inside `packages/ui`, so every copy control on `/en` announced itself in the wrong language — the component now takes an optional `label` and the digest rows pass a translated one. **The other 8 consumers still use the default** |

**Gate (real exit codes, 2026-08-01, after the ColorConverter deep audit):**
`check 0` · `typecheck 0` · `lint 0` · `test 0 (621)` · `tokens 0 (baseline
re-locked: +19, all colour DATA in named constants or hex quoted inside
evidence comments)` · `contrast 0` · `i18n 0` · `build 0` · `packages:build 0`.
Re-run 2026-08-03 after the owner's three reports: **test 0 (629)**, tokens
re-locked 1466 → 1473.
Re-run 2026-08-03 after the self-review above: `check 0` · `lint 0` ·
`typecheck 0` · `test 0 (641)` · `build 0`. Both new tests were proved to FAIL
against the pre-fix wiring before being kept.
Re-run 2026-08-03 after the shared-`Faq` rollout and the `ToolCard`
conversions: `check 0` · `lint 0` · `typecheck 0` · `test 0 (641)` ·
`tokens 0` · `contrast 0` · `i18n 0` · `build 0`.
Re-run 2026-08-03 after tool 6 (Base64): `check 0` · `lint 0` ·
`typecheck 0` · `test 0 (660)` · `tokens 0` · `i18n 0` · `build 0`.
Re-run after the Base64 self-review: `check 0` · `lint 0` · `typecheck 0` ·
`test 0 (662)` · `tokens 0` · `i18n 0` · `build 0`.
Re-run after tool 7 (JWT decoder): `check 0` · `lint 0` · `typecheck 0` ·
`test 0 (680)` · `tokens 0` · `i18n 0` · `build 0`.
Re-run after the JWT self-review: same eight, `test 0 (680)`. One flake seen
and dismissed with evidence: `uzbek-corpus` asserts a 20x linearity bound and
went to 29x under a concurrent build; `packages/transliteration` is untouched
this session and the file passes 3/3 on a quiet machine.
Re-run after tool 8 (URL encoder): `check 0` · `lint 0` · `typecheck 0` ·
`test 0 (699)` · `tokens 0` · `i18n 0` · `build 0`.
Re-run after the URL-encoder UX rework: same eight, `test 0 (704)`.
Re-run after tool 9 (hash generator): `check 0` · `lint 0` · `typecheck 0` ·
`test 0 (750)` · `tokens 0` · `i18n 0` · `build 0`. The MD5 replacement was
verified against RFC 1321 appendix A.5 (7/7) and against node's `crypto` for
every input length 0–200 (201/201) BEFORE it was wired in.
Re-run after tool 13 (device info) and its 7-file deletion:
`check 0` · `lint 0` · `typecheck 0` · `test 0 (868)` ·
`tokens 0 (278 hits REMOVED)` · `contrast 0` · `i18n 0` · `build 0`. Both
locales prerendered with 5/5 FAQ answers and the privacy note in the served
HTML; 0 key-path leaks across all 34 tool pages.
Re-run after the Lorem self-review and the Cyrillic list:
`check 0` · `lint 0` · `typecheck 0` · `test 0 (852)` · `tokens 0` ·
`contrast 0` · `i18n 0` · `build 0`. Both locales prerendered with the sixth
bank documented in the reference table.
Re-run after tool 12 (Lorem Ipsum) and its 3-file deletion:
`check 0` · `lint 0` · `typecheck 0` · `test 0 (849)` ·
`tokens 0 (219 hits REMOVED)` · `contrast 0` · `i18n 0` · `build 0`. Both
locales prerendered with 5/5 FAQ answers and 5/5 banks in the reference table;
0 key-path leaks across all 34 tool pages.
Re-run after the shareable link, 7 platforms and the `/api/og` fix:
`check 0` · `lint 0` · `typecheck 0` · `test 0 (830)` · `tokens 0` ·
`contrast 0` · `i18n 0` · `build 0`. Verified on the built output: 0 key-path
leaks across all 34 prerendered tool pages, 7/7 platforms in the reference
table in both locales.
Re-run after the OG importer, the re-scrape links and the two i18n defects:
`check 0` · `lint 0` · `typecheck 0` · `test 0 (824)` · `tokens 0` ·
`contrast 0` · `i18n 0` · `build 0`. Verified on the built output: **0 key-path
leaks across all 34 prerendered tool pages**, 5/5 FAQ answers real prose.
Re-run after the OG self-review and the `CodeHighlight` rewrite:
`check 0` · `lint 0` · `typecheck 0` · `test 0 (808)` ·
`tokens 0 (188 hits REMOVED)` · `contrast 0` · `i18n 0` · `build 0`.
Re-run after tool 11 (OG meta generator) and the 21-file deletion:
`check 0` · `lint 0` · `typecheck 0` · `test 0 (802)` ·
`tokens 0 (162 hits REMOVED)` · `contrast 0` · `i18n 0` · `build 0`. Both
locales' prerendered HTML checked: 5/5 FAQ questions and answers on the page,
`/en` in English. One earlier full-suite run reported 2 failures under load and
three consecutive clean runs were green — same class as the documented
`uzbek-corpus` linearity flake, not investigated further.
Re-run after tool 10 (UUID generator): `check 0` · `lint 0` · `typecheck 0` ·
`test 0 (779)` · `tokens 0 (35 hits REMOVED)` · `contrast 0` · `i18n 0` ·
`build 0`. The branch was **red on two of those** when this session started —
`typecheck` (4 errors) and `test` (the UUID tool's own assertion). Both
locales' prerendered HTML re-checked: 5/5 FAQ questions and answers on the
page, `/en` in English.
Suite 572 → **621 tests** (+34 unit for the colour maths and exports, +13
integration + 2 for the tool, driven through the rendered UI).

---

## In progress

- `[x]` **SEO Phases 1–2 shipped.** Fabricated `aggregateRating` + invented
  reviews deleted from 18 files; canonical/hreflang made locale-correct via a new
  `src/lib/seo.ts` (`withLocale`), so `/en` and all 229 book pages stop pointing
  at the wrong URL; `/api/og` implemented. Verified in **served HTML**, not just
  in source. One item deferred with a written reason: `<html lang>` — see the
  initiative.
  → `initiatives/seo-and-rendering.md`
- `[x]` **Design system — all 5 phases shipped.** 3-layer tokens, brand hue
  **217°** (Uzbek flag blue), `pnpm contrast` 32/32 in both schemes, and a
  `pnpm tokens` ratchet that now also rejects malformed classes and
  same-colour-on-same-colour text. **5,401 → 2,600** hardcoded hits.
  Remaining work is blocked or out of scope: 629 in the parked tools, 195 in
  `color-names.ts` (colour data, not styling).
  → `initiatives/design-system.md`
- `[x]` **Code structure Phase 1 — done.** `packages/ui` `@/` imports 3 → 0, and
  the `TOOL_COLORS`/`UI_PATTERNS` duplication is closed: one definition in
  `@webiston/ui`, re-exported by `src/constants/ui-constants.ts`.
  → `initiatives/code-structure.md`

## Initiatives

| Initiative | Status | Next phase |
| ---------- | :----: | ---------- |
| [SEO & rendering](initiatives/seo-and-rendering.md) | `[~]` | Phases 1–3 shipped → **Phase 4, payload** (209 KB logo, CLS, message bundle) |
| [Design system](initiatives/design-system.md) | `[~]` | **Phases 18–18c shipped — the reading page + reader shell.** 18c: rail marker rebuilt (concentric track+marker, one sliding bar, hover no longer greys the active row), rail now follows the reader, collapse button aligned, **`--reading-measure` kills the prose reflow on collapse**, top loader on brand colour, theme toggle's dead first click fixed, framer-motion out of the header (**340 → 299 KB gz**). 18: JS 383 → 340 via barrel imports + FlexSearch. Chapter JS **383 → 340 KB gz** (barrel imports, then FlexSearch deferred), client modules **30 → 16**, ToC anchor off-by-165px fixed, `Pagination` rewritten, tokens **−123 hits**. Rendering questions answered in `reference/architecture.md § 5b`. Next: `TutorialLayoutContent` → server (229 client lines), then the 4 decisions above. Earlier: Phases 1–17b — homepage + /tools + /books + search dialog + **book landing pages + reader-shell alignment**. 17b: owner rejected 17's cards (a `border-t` under one-word titles) → homepage card order exactly, third line previews the chapter's own topics; **sidebar rows 80px → 64px, flush with the header logo and footer**; both sticky rails were **9px under the header** (`top-[3.5rem]` vs a 65px bar) → `top-(--header-height)`. Also found a **`pnpm tokens` blind spot** (no `black`/`white` in `PALETTE_RE` — 124 invisible hits). Next: the **book-reader chrome** (`Sidebar`, `TableOfContents`, `Pagination`, `ContentMeta` — still palette classes + raw `#8D8D93`) |
| [Code structure](initiatives/code-structure.md) | `[~]` | Phase 2 — collapse the `src/components/ui/*` shim layer |
| [Tooling, CI & testing](initiatives/tooling-ci-and-testing.md) | `[~]` | **Phase 3 — first tests in `src/`** (Phase 1 CI shipped) |
| [Content & i18n](initiatives/content-and-i18n.md) | `[ ]` | Phase 1 — fix `url-encoder` key parity |

- `[x]` **`globals.css` split by concern (609 → a 21-line entry + 4 files).**
  `src/styles/tokens.css` (design system — the four token blocks were at lines
  9/309/404/472 with ~350 lines of unrelated rules between them),
  `base.css`, `background-pattern.css` (180 lines for ONE component),
  `content.css` (MDX chrome + Shiki). **Proved behaviour-preserving:** the
  compiled CSS holds the same 2,271 rules, 0 added, 0 removed — only the token
  block moved earlier, and nothing it jumped over declares a custom property.
  `scripts/contrast-check.mjs` repointed at the new path.
- `[x]` **`pnpm contrast` could pass while checking nothing.** A pair that failed
  to parse was counted as `SKIP`, printed, and ignored — so a moved or reformatted
  token block would have reported "✓ All contrast requirements pass (32 skipped)"
  and exited **0**. A skip is now a failure. Verified with an injected regression:
  exit 1 with the token block mangled, exit 0 restored.

## Shipped

- `[x]` **Dependency upgrade & tooling hardening** — all 4 workspaces to latest,
  TS 7 adopted, hooks rebuilt (push no longer hangs), 46 artifacts untracked,
  3 stale docs deleted. → `../archive/2026-07_dependency-upgrade-and-tooling.md`

---

## Next up

**One token file, imported by both surfaces.** `apps/extensions/latin-cyrillic/
entrypoints/popup/style.css` now carries the site's real values, but it carries
them as a **copy** — and a copy is what let the extension drift to a different
palette in the first place. The fix is `packages/ui/styles/tokens.css` (the
package already exports `"./styles/*"`, and `packages/*` is the only thing an
extension may import), with the app-only tokens — sidebar, chart, hero — left
in `src/styles/`. It touches the site's global CSS, so it needs its own pass
with before/after screenshots, not a tail-end of an unrelated change.

**The tool portfolio — measured 2026-07-31, awaiting a decision.**
21 modules, **27,695 lines**; 17 routed, 4 parked behind `__`. Owner's GA:
latin-cyrillic is far and away first, qr-generator a distant second, the rest
near zero. That leaves **23,114 lines serving almost no traffic** — and they are
not free, because every one of them carries the same five pending fixes, the
same hardcoded-colour debt, and has to be re-verified on every design change.

The pattern has one explanation: **the site's only moat is the Uzbek language.**
"json formatter" is an English query answered by entrenched global sites, and a
developer types it in English regardless of where they live. "lotin kirill" is a
query only Uzbek speakers make, contested by a handful of small local sites — a
race we can win, and did. Nineteen of the tools do not use the moat.

Proposal, in three parts:

1. **Invest** in latin-cyrillic and qr-generator only.
2. **Freeze** the other routed tools: apply the mechanical fixes (barrel import,
   `/en` locale, width, `GradientTabs`) in one sweep and stop hand-polishing
   surfaces nobody opens.
3. **Decide** on the 4 parked tools — `__http-status`, `__keycode-info`,
   `__user-agent-analyzer`, `__website-status`, **3,848 lines with no route at
   all**. Route them or delete them; today they are pure carry. Deleting needs
   an explicit yes.

Then build on the moat. Best candidate found: **raqamni so'z bilan yozish**
(number → Uzbek words). Every invoice and contract in the country writes the sum
in words; the top search result today is a *code snippet article* on UzbekDevs,
not a working tool, which means nothing good ranks. It is small, it is
Uzbek-only, and it can output in **both scripts** by reusing
`@webiston/transliteration` — which no competitor can copy without our engine.

**Homepage — awaiting one confirmation, then it is done.** The hero copy was
rewritten and needs the owner's yes or a revert. Exact strings to restore are in
git; the new pair is in `messages/common/{uz,en}.json` under `HomePage.title` /
`HomePage.description`.

**A hero visual was considered and declined, with reasons.** The covers exist
(400×525, 53–75 KB each) but: `images.unoptimized: true` makes a hero image the
worst possible LCP placement; the same three covers already appear ~200px below
in the first section, so a split hero would duplicate them; and halving the
column width forces more line breaks in Uzbek, which is long. A centred,
text-first hero is also the correct pattern when the offer is a *collection*
rather than one product with a screenshot. Revisit only after `unoptimized` is
resolved — and then with the real book covers, not an abstract illustration.

**~~41 cards is still too many for a homepage.~~** Resolved in Phase 11 — each
book now shows 5 chapters + an explicit "all chapters" door, and Phase 17 gave
that door a real destination: `/books/<id>` is now the book's own table of
contents rather than a bullet list pointing back at the sidebar.

**Testing — continue the Trophy.** The pure layer is covered (307 tests). Next:
`useOgMetaGenerator` (601 lines) and `useMicrophoneTest` (521) — both need pure
logic pulled out of their `useCallback`s before they can be tested at all — then
the fat layer, driving a whole tool with React Testing Library as a user.

**SEO Phase 4 is nearly done — what remains is one decision, not code.**
`images.unoptimized: true` (`next.config.ts:26`) means the **25 MB of book
figures in `public/` are served raw**; one chapter ships several MB of PNG. That
now dwarfs every other payload item on the site. Owner's call — see `backlog.md`.

The heavy-dep item turned out to be a non-issue: `pdfjs-dist`, `mammoth`, `docx`
and `leaflet` are **already** lazily chunked. Measured in the browser — a tool
page loads 756 KB of JS and **none** of those chunks. `katex` and `shiki` no
longer reach the client at all.

Note the `<html lang>` trade-off is now settled by evidence: static rendering
won, `lang="uz"` stays on the 19 English pages. Revisiting it means giving the
266 prerendered routes back — see the initiative.

**New from Phase 18 — four things found on the reading page that need an explicit
yes before I touch them (deletions and dependency swaps are not drive-by work):**

1. **`ContentMeta` renders an empty bordered strip.** Every child is commented
   out, so its whole contribution to 226 chapter pages is a ~40px hairline. It
   accepts `updatedAt` and never reads it, and `TutorialContent` computes
   `frontmatter.updatedAt || new Date().toISOString()` purely to feed it. Delete
   the component + the prop + the computation, or restore the "improve this page
   on GitHub" link it used to hold?
2. **`CustomParagraph.processChildren` is dead code that runs on every
   paragraph.** It recursively `React.Children.map`s + `cloneElement`s looking
   for `element.type === "div"`, but `MDXContent`'s components map defines a
   `div` override, so the child's type is that FUNCTION, never the string.
   Verified empirically: **0** occurrences of the class it would emit across
   **79** `<p>` elements in a built chapter. The component can collapse to a
   one-line `<p>`.
3. **`next-mdx-remote` is archived** (2026-04-09, "no longer supported") and sits
   on the critical path of 226 pages. Options: stay pinned, move to
   `next-mdx-remote-client`, or follow its own README's advice and compile MDX
   with the core `@mdx-js/mdx` package at build time. Needs its own initiative.
4. **`@next/mdx` config is dead.** `withMDX` + `providerImportSource:
   "@mdx-js/react"` + `pageExtensions: md/mdx` — zero `.mdx` files under
   `src/app/`, `@mdx-js/react` imported nowhere. Removing it also drops
   `@mdx-js/loader` and `@next/mdx` from `package.json`.

**Also from Phase 18, not started:** `TutorialLayoutContent` is a **229-line
`'use client'`** component computing breadcrumbs from `useParams()` when the
layout already has `params` on the server. Moving it server-side means passing
server JSX through the client `TutorialLayout` as a prop, and keeping only the
overflow dropdown as an island. Real win, bigger change.

**New from Phase 17 — needs a decision, not code:** widening `PALETTE_RE` in
`scripts/token-guardrail.mjs` to catch `black`/`white`. It would add **124** hits
to the ratchet in one go (`HttpStatus.tsx` 15, `OgMetaGenerator/PreviewPanel.tsx`
10, `Pagination.tsx` 6, `Sidebar.tsx` 4, …). Some are legitimate — `text-white`
on a brand-coloured surface — so this is a triage job, not a sweep.

**`Tool.color` is dead data.** All 17 entries in `src/constants/ui.ts` carry a
`color: "bg-blue-500/20"`-style palette class and **nothing reads it** — the
tools page colours by category from `--chart-*` tokens now. 17 hardcoded
palette classes in one file, waiting on a yes.

**Unreferenced in the QR module — found during the audit, NOT deleted:**
`CONTENT_TYPES` + `QrContentType` in `constants/index.ts` (0 consumers since the
content-type tabs came out; they are the placeholder for the WiFi/vCard forms),
`UseQrGeneratorResult` (0 consumers), and `errorLevel` on the hook's return
(computed and returned, destructured nowhere). Plus the pre-existing dead i18n
groups: `Sizes`, `ErrorLevels`, `ResultsPanel`, `QrDisplay`, `Presets`,
`CustomizationPanel`, `Categories`, `DataTypes`, `ControlPanel`, `InfoSection`.
All need an explicit yes.

**Two decisions still blocking work:**

0. **Three unreferenced items awaiting a yes/no** —
   `src/components/mdx/CodeBlock/CodeBlockSkeleton.tsx` (orphaned by server-side
   highlighting), `const _inter = Inter(...)` in `(app)/[locale]/layout.tsx:14`
   (a second Inter download the root layout already does properly), and the 8
   keys below.
1. **8 dead `en`-only keys** in `messages/tools/url-encoder/en.json`
   (`Info.formatExample.exampleText`, `exampleEncoded`, `Info.urlStructure.*` ×6).
   Re-verified unused: `InfoSection.tsx` references neither group. This is the
   only red gate (`pnpm i18n` → 1). Deleting needs an explicit yes.
2. **The four parked `__` tools** — 629 of the 2,600 remaining hardcoded-colour
   hits sit in code that has no route.
3. **`src/styles/background-pattern.css` is now orphaned** (3,624 bytes). Its
   import is replaced by `hero.css` and the only remaining mentions are two
   comments. Deleting needs an explicit yes.
4. **Duplicate animation dependency.** `framer-motion` **and** `motion` are both
   declared at `12.43.0` — `motion` is the renamed same package. 27 imports use
   `framer-motion`; **one** uses `motion/react`
   (`packages/ui/src/primitives/typing-animation.tsx:3`). Consolidating means
   picking one and dropping the other from `package.json`.

**Found in passing, not fixed (needs its own decision):**

- **`JwtDecoderPage.InputPanel.clear` namespace resolved** — `JwtDecoder/components/InputPanel.tsx` updated to query `JwtDecoderPage.Input`, matching both `uz.json` and `en.json` bundles cleanly (`clear` is defined in both). Build `MISSING_MESSAGE` 2 → 0.

---

## Ground rules for this file

- One line per item. Evidence (file:line or a number) or it is not an item.
- Detail goes in `initiatives/`; shipped detail goes in `../archive/`.
- Never a bare `- [x] Done` — name what and where.
