/**
 * Search terms for the screen resolution tool.
 *
 * Ten, not the seventy the route used to carry inline. A keyword list that
 * repeats "screen resolution" in six near-identical forms is not targeting
 * more queries — search engines deduplicate it, and the page's own copy is
 * what ranks. These are the phrasings people actually type, in both locales.
 */

// Not `readonly`: Next's `Metadata["keywords"]` takes a mutable `string[]`.
export const PRIMARY_KEYWORDS: string[] = [
  "what is my screen resolution",
  "viewport size checker",
  "browser window size",
  "css breakpoint checker",
  "device pixel ratio",
  "ekran o'lchami",
  "viewport o'lchami",
  "brauzer oynasi o'lchami",
  "css breakpoint tekshirish",
  "piksel zichligi"
]
