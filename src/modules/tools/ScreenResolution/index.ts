// Static sections — Server Components. Imported from their own files rather
// than through components/index.ts so the client barrel never sees them.
export { ScreenFaq } from "./components/ScreenFaq"
export { ScreenReference } from "./components/ScreenReference"
export { default, ScreenResolution } from "./ScreenResolution"
