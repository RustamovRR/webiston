/**
 * Search terms for the IP info tool.
 *
 * Ten, not the ~90 the route used to carry inline. A list that repeats
 * "ip geolocation" in six near-identical forms does not target more queries —
 * search engines deduplicate it, and the page's own copy is what ranks.
 */

// Not `readonly`: Next's `Metadata["keywords"]` takes a mutable `string[]`.
export const PRIMARY_KEYWORDS: string[] = [
  "what is my ip",
  "ip address lookup",
  "ip geolocation",
  "who owns this ip",
  "asn lookup",
  "ip manzilim",
  "ip manzil tekshirish",
  "ip joylashuvi",
  "provayder aniqlash",
  "bepul ip tekshiruvi"
]
