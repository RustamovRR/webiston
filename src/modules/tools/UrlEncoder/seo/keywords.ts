/**
 * Search terms for the URL encoder.
 *
 * What this replaces was ~50 entries across three languages, including a
 * Russian block for a locale the site does not serve. Kept short and true.
 */

// Not `readonly`: Next's `Metadata["keywords"]` takes a mutable `string[]`.
export const PRIMARY_KEYWORDS: string[] = [
  "url encoder",
  "url decoder",
  "url kodlash",
  "url dekodlash",
  "percent encoding",
  "encodeuricomponent",
  "encodeuri",
  "query string decoder",
  "urlencode online",
  "bepul url encoder"
]
