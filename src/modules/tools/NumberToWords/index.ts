// Static section — a Server Component. Imported from its own file rather than
// through components/index.ts so the client barrel never sees it.
export { NumberToWordsFaq } from "./components/NumberToWordsFaq"
export { default, NumberToWords } from "./NumberToWords"
