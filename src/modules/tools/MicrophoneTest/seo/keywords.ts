/**
 * Search terms for the microphone test.
 *
 * Ten, not the ~60 the route used to carry inline in near-identical forms.
 * Search engines deduplicate a repeated phrase; the page's own copy is what
 * ranks, and the FAQ answers the queries this list only points at.
 */

// Not `readonly`: Next's `Metadata["keywords"]` takes a mutable `string[]`.
export const PRIMARY_KEYWORDS: string[] = [
  "microphone test",
  "test my mic",
  "mic not working",
  "online microphone test",
  "check microphone",
  "mikrofon test",
  "mikrofonni tekshirish",
  "mikrofon ishlamayapti",
  "ovoz yozish onlayn",
  "bepul mikrofon sinovi"
]
